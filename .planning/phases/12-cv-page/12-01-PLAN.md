---
phase: 12-cv-page
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/pages/cv.html
  - src/pages/about.html
autonomous: true
requirements:
  - CV-01
  - CV-02
  - CV-03
  - CV-04
  - CV-05

must_haves:
  truths:
    - "CV page work-history section lists 5 distinct timeline items in chronological order (most recent first): Matalan, NUX Volunteer, Santander Product Designer, Santander UX Researcher (rotation), Blake's Estates"
    - "CV page education section lists 2 entries: Google UX Professional Certificate and University of Liverpool BSc Biological Sciences"
    - "CV page CTA section contains a Download CV button that opens or downloads /Samuel-Blake-CV.pdf"
    - "About page CTA section contains a Download CV button that opens or downloads /Samuel-Blake-CV.pdf"
    - "About page no longer contains the TODO comment about the missing Download CV button"
  artifacts:
    - path: "src/pages/cv.html"
      provides: "Complete CV page markup with 5 work-history items, updated education, Download CV CTA"
      contains: "NUX"
    - path: "src/pages/about.html"
      provides: "About page with Download CV button wired in the CTA section"
      contains: "Samuel-Blake-CV.pdf"
  key_links:
    - from: "src/pages/cv.html"
      to: "/Samuel-Blake-CV.pdf"
      via: "anchor with download attribute"
      pattern: 'href="/Samuel-Blake-CV.pdf"'
    - from: "src/pages/about.html"
      to: "/Samuel-Blake-CV.pdf"
      via: "anchor with download attribute"
      pattern: 'href="/Samuel-Blake-CV.pdf"'
---

<objective>
Fill the remaining CV content and wire the PDF download on both the CV and About pages.

Purpose: A recruiter visiting the CV or About page can read Sam's full employment history (including NUX volunteer work and the Santander UX Researcher rotation), see the Google UX Professional Certificate, and download the PDF CV in one click.

Output: cv.html with 5 work-history timeline items, 2 education items, and a Download CV button. about.html with the TODO comment replaced by a Download CV button.
</objective>

<execution_context>
@C:/Users/sambl/OneDrive/Documents/my-portfolio-v2/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/sambl/OneDrive/Documents/my-portfolio-v2/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@./CLAUDE.md
@.claude/CLAUDE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@src/pages/cv.html
@src/pages/about.html

<interfaces>
<!-- Existing BEM patterns used by this plan. Use these exact class names. -->

Timeline item (in cv.html `.about-timeline > li`):
- `<li class="about-timeline__item">`
- `<div class="about-timeline__meta">` containing:
  - `<span class="about-timeline__org">{org}</span>`
  - `<span class="about-timeline__period">{dates}</span>`
- `<h3 class="about-timeline__role">{role}</h3>`
- `<p class="about-timeline__desc">{description}</p>` (omit for education entries with no description, matching existing Liverpool entry)

Button pattern (existing in cv.html):
- `<a href="..." class="btn btn--secondary">...</a>` (secondary action)
- `<a href="..." class="btn btn--primary" target="_blank" rel="noopener noreferrer">...</a>` (primary external)
- Download CV pattern: `<a href="/Samuel-Blake-CV.pdf" class="btn btn--secondary" download>Download CV</a>`

CTA actions container (existing in both files):
- `<div class="cta-section__actions">` wraps the button row
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update cv.html — add NUX + UX Researcher work-history entries, add Google UX cert, add Download CV button (CV-01, CV-02, CV-03, CV-04)</name>
  <files>src/pages/cv.html</files>
  <read_first>
    - src/pages/cv.html (full file — work-history, education, and CTA sections)
    - ./CLAUDE.md (BEM and design-token rules)
  </read_first>
  <action>
Edit `src/pages/cv.html` in three locations. Use ONLY existing BEM classes (`about-timeline__item`, `about-timeline__meta`, `about-timeline__org`, `about-timeline__period`, `about-timeline__role`, `about-timeline__desc`, `btn`, `btn--secondary`, `btn--primary`, `cta-section__actions`). Do NOT add inline styles, hex values, or new CSS files.

**1. Work history section (inside `<ul class="about-timeline" role="list">`):** Replace the existing 3-item list with 5 items in this exact order (most recent first). Keep the `[PLACEHOLDER ...]` descriptions on the existing Matalan, Santander Product Designer, and Blake's Estates entries — do NOT invent copy. Add `[PLACEHOLDER — Sam to add 1–2 sentences]` for the two new entries.

Replace the existing Self-employed `<span class="about-timeline__org">Self-employed</span>` with `<span class="about-timeline__org">Blake's Estates</span>` so the org name matches the roadmap success criterion wording. Keep `<span class="about-timeline__period">2020 — 2022</span>` and `<h3 class="about-timeline__role">Property Director</h3>` as-is.

Final ordered list of `<li class="about-timeline__item">` entries:

  1. org `Matalan` / period `2024 — present` / role `Product Designer` / desc unchanged placeholder
  2. org `NUX` / period `2023 — present` / role `Volunteer` / desc `[PLACEHOLDER — Sam to add 1–2 sentences: nature of NUX volunteering, Manchester / Liverpool community involvement]`
  3. org `Santander UK` / period `2022 — 2024` / role `Product Designer` / desc unchanged placeholder
  4. org `Santander UK` / period `2021 — 2022` / role `UX Researcher (rotation)` / desc `[PLACEHOLDER — Sam to add 1–2 sentences: research rotation scope before moving to Product Designer]`
  5. org `Blake's Estates` / period `2020 — 2022` / role `Property Director` / desc unchanged placeholder

**2. Education section (inside the second `<ul class="about-timeline" role="list">`):** Add the Google certificate AS THE FIRST entry (above University of Liverpool), matching the existing Liverpool entry's structure but with no `<p class="about-timeline__desc">` (Liverpool has none either):

```
<li class="about-timeline__item">
  <div class="about-timeline__meta">
    <span class="about-timeline__org">Google</span>
    <span class="about-timeline__period">2022</span>
  </div>
  <h3 class="about-timeline__role">UX Professional Certificate</h3>
</li>
```

Keep the existing Liverpool entry exactly as-is below it.

**3. CTA section (`<div class="cta-section__actions">`):** Add a Download CV button as the FIRST action in the row (before "Get in touch"). The order from left to right must become: Download CV → Get in touch → Connect on LinkedIn. The Download CV button must use `class="btn btn--secondary"` and the `download` boolean attribute:

```
<a href="/Samuel-Blake-CV.pdf" class="btn btn--secondary" download>Download CV</a>
```

Keep the existing "Get in touch" anchor (`btn--secondary`, href `/contact`) and the "Connect on LinkedIn" anchor (`btn--primary`, target="_blank", rel="noopener noreferrer") unchanged.
  </action>
  <verify>
    <automated>node -e "const fs=require('fs');const h=fs.readFileSync('src/pages/cv.html','utf8');const items=(h.match(/class=\"about-timeline__item\"/g)||[]).length;const checks={'5+2 timeline items (work + education)':items===7,'NUX entry':h.includes('>NUX<'),'UX Researcher rotation':h.includes('UX Researcher (rotation)'),'Blake\\'s Estates':h.includes('Blake&#39;s Estates')||h.includes(\"Blake's Estates\"),'Google UX cert org':h.includes('>Google<'),'Google UX cert role':h.includes('UX Professional Certificate'),'Download CV anchor':/<a[^>]+href=\"\\/Samuel-Blake-CV\\.pdf\"[^>]*download[^>]*>Download CV<\\/a>/.test(h),'No inline styles added':!/<a[^>]*style=/.test(h)};let ok=true;for(const[k,v]of Object.entries(checks)){console.log((v?'OK ':'FAIL ')+k);if(!v)ok=false;}process.exit(ok?0:1);"</automated>
  </verify>
  <acceptance_criteria>
    - `src/pages/cv.html` contains exactly 5 `<li class="about-timeline__item">` entries inside the work-history `<ul class="about-timeline">`
    - `src/pages/cv.html` contains the substring `<span class="about-timeline__org">NUX</span>`
    - `src/pages/cv.html` contains the substring `<h3 class="about-timeline__role">Volunteer</h3>`
    - `src/pages/cv.html` contains the substring `<h3 class="about-timeline__role">UX Researcher (rotation)</h3>`
    - `src/pages/cv.html` contains the substring `<span class="about-timeline__org">Blake's Estates</span>` (the existing `Self-employed` org label has been renamed)
    - `src/pages/cv.html` contains the substring `<span class="about-timeline__org">Google</span>` followed somewhere in the same `<li>` by `<h3 class="about-timeline__role">UX Professional Certificate</h3>`
    - `src/pages/cv.html` contains exactly 2 `<li class="about-timeline__item">` entries inside the education `<ul class="about-timeline">` (Google first, Liverpool second)
    - `src/pages/cv.html` contains the substring `<a href="/Samuel-Blake-CV.pdf" class="btn btn--secondary" download>Download CV</a>` inside `<div class="cta-section__actions">`
    - The Download CV anchor appears before the `/contact` "Get in touch" anchor in document order
    - `src/pages/cv.html` contains no inline `style="..."` attributes
    - `src/pages/cv.html` contains no hex colour values (no `#` followed by 3 or 6 hex digits in element attributes other than meta theme-color)
  </acceptance_criteria>
  <done>cv.html lists all 5 work roles in the timeline, includes the Google UX Professional Certificate as the first education entry, and the CTA actions row starts with a working Download CV button pointing to /Samuel-Blake-CV.pdf.</done>
</task>

<task type="auto">
  <name>Task 2: Update about.html — replace TODO comment with Download CV button (CV-05)</name>
  <files>src/pages/about.html</files>
  <read_first>
    - src/pages/about.html (full file — focus on the CTA section)
    - ./CLAUDE.md (BEM and design-token rules)
  </read_first>
  <action>
Edit `src/pages/about.html` inside `<div class="cta-section__actions">` (the only `cta-section__actions` block in the file, at the bottom of `<main>`).

Replace this exact line:
```
            <!-- TODO: add Download CV button once /public/assets/cv/samuel-blake-cv.pdf exists -->
```

with this Download CV anchor (matching the cv.html button exactly — same class, same href, same download attribute, same text):
```
            <a href="/Samuel-Blake-CV.pdf" class="btn btn--secondary" download>Download CV</a>
```

The Download CV anchor must appear BEFORE the existing "Connect on LinkedIn" anchor so the action row reads left-to-right: Download CV → Connect on LinkedIn. Do NOT modify the LinkedIn anchor. Do NOT introduce inline styles, new CSS files, or new JS.
  </action>
  <verify>
    <automated>node -e "const fs=require('fs');const h=fs.readFileSync('src/pages/about.html','utf8');const checks={'TODO comment removed':!h.includes('TODO: add Download CV button'),'Download CV anchor present':/<a[^>]+href=\"\\/Samuel-Blake-CV\\.pdf\"[^>]*download[^>]*>Download CV<\\/a>/.test(h),'btn--secondary class on Download CV':/<a[^>]*class=\"btn btn--secondary\"[^>]*href=\"\\/Samuel-Blake-CV\\.pdf\"|<a[^>]*href=\"\\/Samuel-Blake-CV\\.pdf\"[^>]*class=\"btn btn--secondary\"/.test(h),'LinkedIn anchor still present':h.includes('linkedin.com/in/samuel-blake-224605186/'),'Download CV before LinkedIn':h.indexOf('Samuel-Blake-CV.pdf')<h.indexOf('linkedin.com/in/samuel-blake-224605186/'),'No inline styles added':!/<a[^>]*style=/.test(h)};let ok=true;for(const[k,v]of Object.entries(checks)){console.log((v?'OK ':'FAIL ')+k);if(!v)ok=false;}process.exit(ok?0:1);"</automated>
  </verify>
  <acceptance_criteria>
    - `src/pages/about.html` no longer contains the substring `TODO: add Download CV button`
    - `src/pages/about.html` contains the substring `<a href="/Samuel-Blake-CV.pdf" class="btn btn--secondary" download>Download CV</a>`
    - The Download CV anchor sits inside the `<div class="cta-section__actions">` block (i.e. between `<div class="cta-section__actions">` and its closing `</div>`)
    - The Download CV anchor appears before the existing LinkedIn anchor (`linkedin.com/in/samuel-blake-224605186/`) in document order
    - The existing LinkedIn anchor (`class="btn btn--primary"`, `target="_blank"`, `rel="noopener noreferrer"`) is unchanged
    - `src/pages/about.html` contains no inline `style="..."` attributes
    - No new files are created in this task
  </acceptance_criteria>
  <done>about.html CTA section shows Download CV before Connect on LinkedIn, the TODO comment is gone, and the Download CV anchor points to /Samuel-Blake-CV.pdf with the download attribute.</done>
</task>

</tasks>

<verification>
Run from project root after both tasks:

```
node -e "const fs=require('fs');for(const f of ['src/pages/cv.html','src/pages/about.html']){const h=fs.readFileSync(f,'utf8');console.log(f,/<a[^>]+href=\"\\/Samuel-Blake-CV\\.pdf\"[^>]*download[^>]*>Download CV<\\/a>/.test(h)?'OK':'FAIL');}"
```

Both files must report `OK`.

Manual smoke (optional, executor): `npm run dev`, visit `/cv` and `/about`, confirm the Download CV button appears and clicking it serves `Samuel-Blake-CV.pdf`.
</verification>

<success_criteria>
1. cv.html `<ul class="about-timeline">` work-history list contains exactly 5 `<li class="about-timeline__item">` entries covering Matalan, NUX, Santander Product Designer, Santander UX Researcher (rotation), and Blake's Estates.
2. cv.html education list contains 2 entries with Google UX Professional Certificate above University of Liverpool.
3. cv.html CTA actions row starts with `<a href="/Samuel-Blake-CV.pdf" class="btn btn--secondary" download>Download CV</a>`.
4. about.html CTA actions row starts with the same Download CV anchor; the TODO comment is gone.
5. Both files contain no inline `style=` attributes and no hex colours outside `_variables.css`.
</success_criteria>

<output>
Create `.planning/phases/12-cv-page/12-01-SUMMARY.md` when done.
</output>
