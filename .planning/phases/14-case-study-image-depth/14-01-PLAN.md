---
phase: 14-case-study-image-depth
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/pages/case-studies/i-exchange.html
autonomous: true
requirements:
  - IMG-03

must_haves:
  truths:
    - "i-Exchange case study displays real images at every inserted image slot"
    - "Each image has descriptive alt text matching its content"
    - "image-block, before-after, and process-steps components are used at narrative-appropriate points"
    - "No broken img references — src paths point to /assets/images/ with placeholder comments where upload is pending"
    - "All image components render correctly at 375px, 600px, and 1240px"
  artifacts:
    - path: "src/pages/case-studies/i-exchange.html"
      provides: "Image markup using .image-block, .before-after, .process-steps components"
      contains: "image-block"
  key_links:
    - from: "i-exchange.html .cs-phase sections"
      to: "/assets/images/i-exchange/"
      via: "<img src> attributes"
      pattern: "/assets/images/i-exchange/"
---

<objective>
Add all image components to the i-Exchange case study at narrative-appropriate points within the existing cs-phase sections.

Purpose: The i-Exchange case study is copy-only. Adding visual evidence — screenshots of the old vs new UI, search comparison, homepage redesign — gives recruiters proof of the design work described in the text.
Output: i-exchange.html with image-block, before-after, and process-steps markup inserted at 6–8 locations across the narrative phases.
</objective>

<execution_context>
@C:/Users/sambl/OneDrive/Documents/my-portfolio-v2/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/sambl/OneDrive/Documents/my-portfolio-v2/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@C:/Users/sambl/OneDrive/Documents/my-portfolio-v2/.planning/ROADMAP.md

Existing component CSS (already complete — do not modify):
- src/styles/3-components/_image-block.css  — .image-block--contained, --full, --aside; .image-block__caption
- src/styles/3-components/_before-after.css — .before-after, .before-after__col, .before-after__col--before/--after, .before-after__label
- src/styles/3-components/_process-steps.css — .process-steps, .process-steps__item, .process-steps__heading, .process-steps__body

Asset path convention: all images live under /assets/images/i-exchange/
</context>

<prerequisite>
Before running npm run build or visual verification, the user must upload images to public/assets/images/i-exchange/.

Suggested image names (executor must use exactly these src paths in the HTML):
- /assets/images/i-exchange/old-homepage.png       — screenshot of the original i-Exchange homepage
- /assets/images/i-exchange/new-homepage.png        — screenshot of the redesigned homepage
- /assets/images/i-exchange/old-search.png          — screenshot of old exact-match search (zero results on typo)
- /assets/images/i-exchange/new-search.png          — screenshot of new AI search with genius suggestion
- /assets/images/i-exchange/saved-audience.png      — saved audience selector UI screenshot
- /assets/images/i-exchange/search-test-results.png — the NPS-framed search testing results (table or chart)
- /assets/images/i-exchange/article-improvements.png — article page with version history / feedback / accordions

Until uploaded, each img element's src must be empty: src="" with an HTML comment directly above it naming the expected image file, e.g.:
<!-- IMAGE: /assets/images/i-exchange/old-homepage.png -->
<img src="" alt="..." loading="lazy" />

This keeps the markup correct and makes it trivial to fill in the src after upload.
</prerequisite>

<tasks>

<task type="auto">
  <name>Task 1: Insert image slots into Empathise and Define phases</name>
  <files>src/pages/case-studies/i-exchange.html</files>
  <read_first>
    - src/pages/case-studies/i-exchange.html (full file — read before any edits)
    - src/styles/3-components/_image-block.css
    - src/styles/3-components/_before-after.css
  </read_first>
  <action>
    Within the Empathise phase (aria-labelledby="phase-empathise"), after the "What the heuristic analysis found" section's final paragraph (the one ending "before searching a page"), insert a before-after component showing the old vs new homepage:

    The before-after wrapper goes directly inside .cs-phase, after the closing </div> of the second .cs-phase__body block. Structure:

    <div class="before-after" role="img" aria-label="i-Exchange homepage: before and after redesign">
      <div class="before-after__col before-after__col--before">
        <span class="before-after__label">Before</span>
        <!-- IMAGE: /assets/images/i-exchange/old-homepage.png -->
        <img src="" alt="Original i-Exchange homepage showing cluttered navigation, decorative hero, and mixed communications list" loading="lazy" />
      </div>
      <div class="before-after__col before-after__col--after">
        <span class="before-after__label">After</span>
        <!-- IMAGE: /assets/images/i-exchange/new-homepage.png -->
        <img src="" alt="Redesigned i-Exchange homepage with saved audience selector, My Processes section, and separated communications list" loading="lazy" />
      </div>
    </div>

    Within the Define phase (aria-labelledby="phase-define"), after the paragraph ending "a design failure" (inside the "Pain point 2" section), insert a contained image-block for the search comparison:

    <figure class="image-block image-block--contained">
      <!-- IMAGE: /assets/images/i-exchange/old-search.png -->
      <img src="" alt="Old i-Exchange search returning zero results for a misspelled query" loading="lazy" />
      <figcaption class="image-block__caption">Old exact-match search: a single typo voided the entire query with no suggestions.</figcaption>
    </figure>

    Use figure/figcaption for image-block elements throughout (not div). The before-after wrapper uses div (it has its own role+aria-label).
  </action>
  <verify>
    <automated>grep -c "before-after" src/pages/case-studies/i-exchange.html</automated>
  </verify>
  <acceptance_criteria>
    - grep output is >= 2 (wrapper div + at least one col class)
    - before-after component is positioned after the Empathise heuristic analysis body block
    - image-block--contained figure is present in the Define phase
    - Both img elements have non-empty alt text
    - Both img src values are empty strings with placeholder comments directly above them
    - No inline styles added
  </acceptance_criteria>
  <done>Empathise phase contains a before-after (old/new homepage); Define phase contains an image-block for the old search screenshot.</done>
</task>

<task type="auto">
  <name>Task 2: Insert image slots into Ideate, Test, and Solution phases</name>
  <files>src/pages/case-studies/i-exchange.html</files>
  <read_first>
    - src/pages/case-studies/i-exchange.html (already read in Task 1 — use the version in context)
    - src/styles/3-components/_process-steps.css
    - src/styles/3-components/_image-block.css
  </read_first>
  <action>
    In the Test phase (aria-labelledby="phase-test"), after the callout blockquote (the one about "AI search improved NPS by 5% on day one"), insert a contained image-block for the search testing results:

    <figure class="image-block image-block--contained">
      <!-- IMAGE: /assets/images/i-exchange/search-test-results.png -->
      <img src="" alt="Table comparing old search engine vs AI search NPS scores across 4,000 test queries, with and without audience filter" loading="lazy" />
      <figcaption class="image-block__caption">4,000 manual test searches graded as promoter, neutral, or detractor — same NPS framing as the live system.</figcaption>
    </figure>

    In the Solution phase (aria-labelledby="phase-solution"), after the "Home" unordered list (the five bullet list ending with "simplified navigation"), insert a process-steps component showing the dual-track delivery approach. This visualises the delivery method described in the Implementation phase and provides visual interest to the solution section:

    <ol class="process-steps" aria-label="Key homepage improvements delivered">
      <li class="process-steps__item">
        <div>
          <p class="process-steps__heading">Saved audience &amp; AI search</p>
          <p class="process-steps__body">Audience selector surfaced on the homepage, filtering all searches, communications, and content automatically once set.</p>
        </div>
      </li>
      <li class="process-steps__item">
        <div>
          <p class="process-steps__heading">My Processes</p>
          <p class="process-steps__body">Personalised section showing each colleague's favourite and most-viewed articles — no searching for regularly-used content.</p>
        </div>
      </li>
      <li class="process-steps__item">
        <div>
          <p class="process-steps__heading">Communications redesigned</p>
          <p class="process-steps__body">Incidents and general updates separated, colour-coded, and icon-differentiated. Saved audience filters both lists automatically.</p>
        </div>
      </li>
      <li class="process-steps__item">
        <div>
          <p class="process-steps__heading">AI search engine</p>
          <p class="process-steps__body">Machine learning, natural language processing, autocorrect, genius suggestions, and full analytics on knowledge gaps and failed searches.</p>
        </div>
      </li>
    </ol>

    This process-steps sits directly inside the .cs-phase for the Solution section, after the "Home" ul list and before the "Search" h3.

    Also insert a contained image-block for the new search UI, after the "Search" unordered list (the four bullet list ending with "open-in-new-tab now available"):

    <figure class="image-block image-block--contained">
      <!-- IMAGE: /assets/images/i-exchange/new-search.png -->
      <img src="" alt="Redesigned i-Exchange search page with AI-powered engine, audience filter, and richer result cards" loading="lazy" />
      <figcaption class="image-block__caption">AI search with genius suggestions, autocorrect, and a persistent audience filter in the search bar.</figcaption>
    </figure>

    Also insert a contained image-block for the saved audience UI, after the process-steps (before the "Search" h3):

    <figure class="image-block image-block--contained">
      <!-- IMAGE: /assets/images/i-exchange/saved-audience.png -->
      <img src="" alt="i-Exchange homepage showing saved audience selector and personalised My Processes section" loading="lazy" />
      <figcaption class="image-block__caption">Saved audience: set once, filters everything automatically.</figcaption>
    </figure>
  </action>
  <verify>
    <automated>grep -c "image-block" src/pages/case-studies/i-exchange.html</automated>
  </verify>
  <acceptance_criteria>
    - grep output is >= 6 (3 figure elements × 2 class occurrences each = 6 minimum)
    - process-steps ol is present with 4 li.process-steps__item elements
    - search test results image-block is in the Test phase, after the NPS callout
    - saved-audience image-block is in the Solution phase, after the process-steps
    - new-search image-block is in the Solution phase, after the Search ul
    - All img alt text is descriptive (not empty)
    - All img src values are empty strings with placeholder comments
    - No inline styles, no hex values
  </acceptance_criteria>
  <done>Test phase has search-testing image; Solution phase has process-steps summary, saved-audience screenshot, and new-search screenshot.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| filesystem → browser | Static HTML served by Vite — no user input, no server-side processing |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-14-01 | Information Disclosure | img alt text | accept | Alt text describes UI screenshots — no PII, internal tool names already in body copy |
| T-14-02 | Tampering | img src paths | mitigate | Paths use root-relative /assets/images/ — no user-controlled input, no dynamic src construction |
| T-14-SC | Tampering | npm/pip installs | accept | No new packages in this plan |
</threat_model>

<verification>
After the user uploads images to public/assets/images/i-exchange/ and fills in the src attributes:

1. Run `npm run dev` and open http://localhost:5173/src/pages/case-studies/i-exchange.html
2. Scroll through all phases — confirm images render at expected sizes
3. Resize to 375px — before-after should stack vertically, image-blocks should be full-width
4. Resize to 905px+ — before-after should display side-by-side
5. Confirm no broken image slots (no empty grey boxes)
6. Confirm process-steps renders with numbered circles and connector lines at 905px+
</verification>

<success_criteria>
- i-exchange.html contains before-after in Empathise, image-blocks in Define/Test/Solution, and process-steps in Solution
- All img src values are either filled (post-upload) or empty strings with placeholder comments (pre-upload)
- All components use correct BEM classes matching the existing CSS
- No inline styles, no hex values, no JS
- Build passes: `npm run build` exits 0
</success_criteria>

<output>
Create .planning/phases/14-case-study-image-depth/14-01-SUMMARY.md when done
</output>
