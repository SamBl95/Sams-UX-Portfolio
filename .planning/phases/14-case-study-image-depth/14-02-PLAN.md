---
phase: 14-case-study-image-depth
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - src/pages/case-studies/cassi.html
autonomous: true
requirements:
  - IMG-04

must_haves:
  truths:
    - "Cassi case study displays real images at every inserted image slot"
    - "Each image has descriptive alt text matching its content"
    - "before-after component shows the old vs new Cassi feedback UI"
    - "image-block is used to show the repositioned audience filter"
    - "No broken img references — src paths point to /assets/images/cassi/ with placeholder comments where upload is pending"
    - "All image components render correctly at 375px, 600px, and 1240px"
  artifacts:
    - path: "src/pages/case-studies/cassi.html"
      provides: "Image markup using .image-block and .before-after components"
      contains: "before-after"
  key_links:
    - from: "cassi.html .cs-phase sections"
      to: "/assets/images/cassi/"
      via: "<img src> attributes"
      pattern: "/assets/images/cassi/"
---

<objective>
Add all image components to the Cassi case study at narrative-appropriate points within the existing cs-phase sections.

Purpose: Cassi is a UI/UX redesign story — the before/after of the feedback thumbs, the repositioned audience filter, and the rebrand are the evidence. Adding screenshots makes the design decisions concrete and credible.
Output: cassi.html with image-block and before-after markup inserted at 4–5 locations.
</objective>

<execution_context>
@C:/Users/sambl/OneDrive/Documents/my-portfolio-v2/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/sambl/OneDrive/Documents/my-portfolio-v2/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@C:/Users/sambl/OneDrive/Documents/my-portfolio-v2/.planning/ROADMAP.md

Existing component CSS (already complete — do not modify):
- src/styles/3-components/_image-block.css  — .image-block--contained, --full, --aside; .image-block__caption
- src/styles/3-components/_before-after.css — .before-after, .before-after__col, .before-after__col--before/--after, .before-after__label

Asset path convention: all images live under /assets/images/cassi/
</context>

<prerequisite>
Before running npm run build or visual verification, the user must upload images to public/assets/images/cassi/.

Suggested image names (executor must use exactly these src paths in the HTML):
- /assets/images/cassi/old-feedback.png        — old Cassi UI with buried thumbs up/down feedback icons
- /assets/images/cassi/new-feedback.png         — redesigned Cassi UI with clear feedback, toast, Community link
- /assets/images/cassi/old-audience-filter.png  — original audience filter at the top of the screen
- /assets/images/cassi/new-audience-filter.png  — repositioned audience filter directly above query input
- /assets/images/cassi/cassi-rebrand.png         — the winning Cassi logo by Aishat Arowosegbe

Until uploaded, each img element's src must be empty: src="" with an HTML comment directly above it, e.g.:
<!-- IMAGE: /assets/images/cassi/old-feedback.png -->
<img src="" alt="..." loading="lazy" />
</prerequisite>

<tasks>

<task type="auto">
  <name>Task 1: Insert image slots into Empathise and Define phases</name>
  <files>src/pages/case-studies/cassi.html</files>
  <read_first>
    - src/pages/case-studies/cassi.html (full file — read before any edits)
    - src/styles/3-components/_before-after.css
    - src/styles/3-components/_image-block.css
  </read_first>
  <action>
    In the Empathise phase (aria-labelledby="phase-empathise"), after the "Heuristic analysis" paragraph (the one ending "functional, but not ready for the scale of rollout being planned"), insert a contained image-block showing the original Cassi UI:

    <figure class="image-block image-block--contained">
      <!-- IMAGE: /assets/images/cassi/old-feedback.png -->
      <img src="" alt="Original Cassi UI showing feedback thumbs buried between chat responses with minimal visual separation" loading="lazy" />
      <figcaption class="image-block__caption">The original feedback entry point: visually buried, easy to miss mid-conversation.</figcaption>
    </figure>

    In the Define phase (aria-labelledby="phase-define"), after the "Problem 3 — Audience selection" paragraph (the one ending "Making impossible to miss"), insert a before-after component showing the old vs new audience filter position:

    <div class="before-after" role="img" aria-label="Cassi audience filter: before and after repositioning">
      <div class="before-after__col before-after__col--before">
        <span class="before-after__label">Before</span>
        <!-- IMAGE: /assets/images/cassi/old-audience-filter.png -->
        <img src="" alt="Original Cassi interface with audience filter at the top of the screen, far from the query input at the bottom" loading="lazy" />
      </div>
      <div class="before-after__col before-after__col--after">
        <span class="before-after__label">After</span>
        <!-- IMAGE: /assets/images/cassi/new-audience-filter.png -->
        <img src="" alt="Redesigned Cassi interface with audience filter repositioned directly above the query input" loading="lazy" />
      </div>
    </div>

    Use figure/figcaption for image-block elements. The before-after wrapper uses div with role and aria-label.
  </action>
  <verify>
    <automated>grep -c "before-after" src/pages/case-studies/cassi.html</automated>
  </verify>
  <acceptance_criteria>
    - grep output is >= 2
    - image-block figure is in the Empathise phase, after the heuristic analysis paragraph
    - before-after is in the Define phase, after the Problem 3 paragraph
    - before-after has both --before and --after col variants
    - All img alt text is descriptive
    - All img src values are empty strings with placeholder comments
    - No inline styles
  </acceptance_criteria>
  <done>Empathise phase has original Cassi UI screenshot; Define phase has audience filter before-after.</done>
</task>

<task type="auto">
  <name>Task 2: Insert image slots into Solution phase</name>
  <files>src/pages/case-studies/cassi.html</files>
  <read_first>
    - src/pages/case-studies/cassi.html (already read in Task 1 — use the version in context)
    - src/styles/3-components/_before-after.css
    - src/styles/3-components/_image-block.css
  </read_first>
  <action>
    In the Solution phase (aria-labelledby="phase-solution"), insert image components at three points:

    1. After the "Audience filter" h3 and its paragraph (ending "...that instructions and training never could"), insert a contained image-block:

    <figure class="image-block image-block--contained">
      <!-- IMAGE: /assets/images/cassi/new-audience-filter.png -->
      <img src="" alt="Redesigned Cassi interface with audience filter directly above the query input field" loading="lazy" />
      <figcaption class="image-block__caption">Law of proximity at work: filter and input share the same visual zone.</figcaption>
    </figure>

    2. After the "Feedback journey" h3 and its paragraph (ending "...for colleagues who want to leave more detailed context"), insert a before-after showing old vs new feedback UI:

    <div class="before-after" role="img" aria-label="Cassi feedback UI: before and after redesign">
      <div class="before-after__col before-after__col--before">
        <span class="before-after__label">Before</span>
        <!-- IMAGE: /assets/images/cassi/old-feedback.png -->
        <img src="" alt="Original Cassi feedback: three vague options with no visual prominence" loading="lazy" />
      </div>
      <div class="before-after__col before-after__col--after">
        <span class="before-after__label">After</span>
        <!-- IMAGE: /assets/images/cassi/new-feedback.png -->
        <img src="" alt="Redesigned Cassi feedback: clear thumbs up/down, toast confirmation, Community forum link" loading="lazy" />
      </div>
    </div>

    3. After the "Cassi rebrand" h3 and its paragraph (ending "...dismissed it as an unfinished internal tool"), insert a contained image-block for the rebrand:

    <figure class="image-block image-block--contained">
      <!-- IMAGE: /assets/images/cassi/cassi-rebrand.png -->
      <img src="" alt="New Cassi logo and visual identity designed by Aishat Arowosegbe, winner of the internal rebrand competition" loading="lazy" />
      <figcaption class="image-block__caption">Winning rebrand by Aishat Arowosegbe — a visual identity that matched the maturity of the redesigned product.</figcaption>
    </figure>
  </action>
  <verify>
    <automated>grep -c "image-block" src/pages/case-studies/cassi.html</automated>
  </verify>
  <acceptance_criteria>
    - grep output is >= 6 (3 figure elements × 2 class occurrences each minimum)
    - 2 before-after components total in the file (Define + Solution)
    - Solution phase contains: audience filter image-block, feedback before-after, rebrand image-block
    - All img alt text is descriptive
    - All img src values are empty strings with placeholder comments
    - No inline styles, no hex values
    - Build passes: npm run build exits 0
  </acceptance_criteria>
  <done>Solution phase has audience filter screenshot, feedback before-after, and rebrand screenshot. All image slots present across the full Cassi case study.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| filesystem → browser | Static HTML served by Vite — no user input |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-14-01 | Information Disclosure | img alt text | accept | Alt text describes UI screenshots — no PII, internal tool UI descriptions already in body copy |
| T-14-02 | Tampering | img src paths | mitigate | Root-relative /assets/images/ paths — no user-controlled input |
| T-14-SC | Tampering | npm/pip installs | accept | No new packages in this plan |
</threat_model>

<verification>
After the user uploads images to public/assets/images/cassi/ and fills in the src attributes:

1. Run `npm run dev` and open http://localhost:5173/src/pages/case-studies/cassi.html
2. Scroll through all phases — confirm images render at expected sizes
3. Resize to 375px — before-after components should stack vertically
4. Resize to 905px+ — before-after should display side-by-side
5. Confirm rebrand image is contained and captioned correctly
6. Confirm no broken image slots
</verification>

<success_criteria>
- cassi.html contains image-block and before-after components at 5 insertion points across Empathise, Define, and Solution phases
- All img src values are either filled (post-upload) or empty strings with placeholder comments (pre-upload)
- All components use correct BEM classes matching the existing CSS
- No inline styles, no hex values, no JS
- Build passes: `npm run build` exits 0
</success_criteria>

<output>
Create .planning/phases/14-case-study-image-depth/14-02-SUMMARY.md when done
</output>
