---
phase: 11-seo
plan: 02
type: execute
wave: 2
depends_on:
  - 11-01
files_modified:
  - index.html
  - src/pages/about.html
  - public/sitemap.xml
  - public/robots.txt
  - vercel.json
autonomous: true
requirements:
  - SEO-07
  - SEO-08
  - SEO-09
  - SEO-10

must_haves:
  truths:
    - "index.html and about.html contain valid JSON-LD Person schema verifiable via Google Rich Results Test"
    - "public/sitemap.xml lists all 10 pages with clean https://www.samsux.co.uk URLs"
    - "public/robots.txt allows all crawlers and references the sitemap URL"
    - "vercel.json enables clean URL routing so /about serves the about page without a .html extension"
  artifacts:
    - path: "public/sitemap.xml"
      provides: "XML sitemap for all 10 pages"
      contains: "<urlset"
    - path: "public/robots.txt"
      provides: "Crawler permissions and sitemap pointer"
      contains: "Sitemap:"
    - path: "vercel.json"
      provides: "Clean URL rewrite config"
      contains: "cleanUrls"
    - path: "index.html"
      provides: "JSON-LD Person schema for homepage"
      contains: "application/ld+json"
    - path: "src/pages/about.html"
      provides: "JSON-LD Person schema for about page"
      contains: "application/ld+json"
  key_links:
    - from: "public/robots.txt"
      to: "public/sitemap.xml"
      via: "Sitemap: https://www.samsux.co.uk/sitemap.xml"
      pattern: "Sitemap: https"
    - from: "vercel.json"
      to: "Vercel build output"
      via: "cleanUrls: true — Vercel strips .html from output file paths"
      pattern: "cleanUrls"
---

<objective>
Add JSON-LD Person schema to index.html and about.html, create public/sitemap.xml and public/robots.txt, and create vercel.json with cleanUrls enabled.

Purpose: Enables structured data for Google Rich Results, submits a complete sitemap for crawl coverage, and activates clean URL routing so every page is served without a .html extension on samsux.co.uk.
Output: JSON-LD in 2 files, 2 new files in public/, 1 new vercel.json at project root.
</objective>

<execution_context>
@C:/Users/sambl/OneDrive/Documents/my-portfolio-v2/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/sambl/OneDrive/Documents/my-portfolio-v2/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: JSON-LD Person schema in index.html and about.html</name>
  <files>index.html, src/pages/about.html</files>
  <read_first>
    index.html — read the full head block (Plan 01 will have already added canonical and OG tags); insert JSON-LD after the og:image meta and before the closing </head>
    src/pages/about.html — same — read the full updated head block from Plan 01 to find the correct insert point
  </read_first>
  <action>
    In BOTH index.html and src/pages/about.html, insert the following script block immediately before the closing </head> tag. The block is identical in both files — the Person schema describes Sam Blake the person, not a specific page.

    Insert this exact block:

    <script type="application/ld+json">
    {
      "@context": "https://schema.org",
      "@type": "Person",
      "name": "Sam Blake",
      "jobTitle": "Product Designer",
      "url": "https://www.samsux.co.uk",
      "email": "mailto:sam.blake@outlook.com",
      "sameAs": [
        "https://www.linkedin.com/in/samblakedesign"
      ]
    }
    </script>

    Place it as the LAST element inside <head>, directly before </head>. Do not place it in the <body>.

    NOTE on sameAs LinkedIn URL: use "https://www.linkedin.com/in/samblakedesign" as the placeholder. If Sam's actual LinkedIn URL differs, it should be updated manually — the plan uses this placeholder so the schema is structurally valid.
  </action>
  <verify>
    grep -c 'application/ld+json' index.html src/pages/about.html
    grep -c 'schema.org' index.html src/pages/about.html
  </verify>
  <acceptance_criteria>
    - index.html contains exactly one script[type="application/ld+json"] block
    - src/pages/about.html contains exactly one script[type="application/ld+json"] block
    - Both blocks contain: "@type": "Person", "name": "Sam Blake", "jobTitle": "Product Designer", "url": "https://www.samsux.co.uk", "email": "mailto:sam.blake@outlook.com", "sameAs"
    - The JSON-LD block appears inside <head>, not inside <body>
    - npx vite build exits 0
  </acceptance_criteria>
  <done>index.html and about.html contain valid JSON-LD Person schema; both pages pass Google Rich Results Test structural validation</done>
</task>

<task type="auto">
  <name>Task 2: Create public/sitemap.xml, public/robots.txt, and vercel.json</name>
  <files>public/sitemap.xml, public/robots.txt, vercel.json</files>
  <read_first>
    vite.config.js — confirm all 10 declared pages to ensure sitemap coverage is complete
    public/ directory listing — confirm no existing sitemap.xml or robots.txt before creating
  </read_first>
  <action>
    CREATE public/sitemap.xml with the following exact content. All URLs use the clean production path (no .html extension). lastmod is today's date 2026-05-19. changefreq and priority are omitted — they are ignored by Google.

    <?xml version="1.0" encoding="UTF-8"?>
    <urlset xmlns="http://www.sitemaps.org/schemas/sitemap/0.9">
      <url>
        <loc>https://www.samsux.co.uk/</loc>
        <lastmod>2026-05-19</lastmod>
      </url>
      <url>
        <loc>https://www.samsux.co.uk/about</loc>
        <lastmod>2026-05-19</lastmod>
      </url>
      <url>
        <loc>https://www.samsux.co.uk/contact</loc>
        <lastmod>2026-05-19</lastmod>
      </url>
      <url>
        <loc>https://www.samsux.co.uk/cv</loc>
        <lastmod>2026-05-19</lastmod>
      </url>
      <url>
        <loc>https://www.samsux.co.uk/work</loc>
        <lastmod>2026-05-19</lastmod>
      </url>
      <url>
        <loc>https://www.samsux.co.uk/case-studies/i-exchange</loc>
        <lastmod>2026-05-19</lastmod>
      </url>
      <url>
        <loc>https://www.samsux.co.uk/case-studies/cassi</loc>
        <lastmod>2026-05-19</lastmod>
      </url>
      <url>
        <loc>https://www.samsux.co.uk/case-studies/community</loc>
        <lastmod>2026-05-19</lastmod>
      </url>
      <url>
        <loc>https://www.samsux.co.uk/stories</loc>
        <lastmod>2026-05-19</lastmod>
      </url>
      <url>
        <loc>https://www.samsux.co.uk/stories/design-systems-and-portfolio-sites</loc>
        <lastmod>2026-05-19</lastmod>
      </url>
    </urlset>

    CREATE public/robots.txt with the following exact content:

    User-agent: *
    Allow: /

    Sitemap: https://www.samsux.co.uk/sitemap.xml

    CREATE vercel.json at the project root (same level as package.json and vite.config.js) with the following exact content. cleanUrls removes .html from all output file paths — this is the Vercel-native solution and requires no manual rewrite rules per page.

    {
      "cleanUrls": true
    }

    Do NOT add trailingSlash, rewrites, or redirects unless the file already contains them. The single cleanUrls flag is sufficient for this Vite MPA build.
  </action>
  <verify>
    grep -c '<url>' public/sitemap.xml
    grep -c 'Sitemap:' public/robots.txt
    grep -c 'cleanUrls' vercel.json
  </verify>
  <acceptance_criteria>
    - public/sitemap.xml exists and contains exactly 10 <url> entries
    - Every <loc> in sitemap.xml uses https://www.samsux.co.uk and has no .html extension
    - sitemap.xml is valid XML (no unclosed tags, no duplicate entries)
    - public/robots.txt exists and contains "User-agent: *", "Allow: /", and "Sitemap: https://www.samsux.co.uk/sitemap.xml"
    - vercel.json exists at the project root and contains "cleanUrls": true
    - vercel.json is valid JSON (parseable with JSON.parse)
    - npx vite build exits 0 (vercel.json does not affect the Vite build — confirming the build still passes)
  </acceptance_criteria>
  <done>sitemap.xml lists all 10 clean-path URLs, robots.txt allows all crawlers and points to the sitemap, vercel.json enables clean URL routing for the production Vercel deployment</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| public/ → Vercel CDN | Files in public/ are served as-is at the root; sitemap and robots.txt must not expose sensitive paths |
| vercel.json → Vercel router | cleanUrls affects all routes in the deployment; misconfiguration could break navigation |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-11-07 | Information Disclosure | robots.txt | accept | robots.txt is public by design; no sensitive paths exist in this static portfolio |
| T-11-08 | Information Disclosure | JSON-LD email | accept | sam.blake@outlook.com is already public in contact.html — schema adds no new exposure |
| T-11-09 | Tampering | vercel.json | mitigate | cleanUrls only — no rewrites or redirects that could be exploited for open redirect |
| T-11-10 | Denial of Service | sitemap.xml crawler traffic | accept | Static file; no server-side processing; Vercel CDN handles load |
| T-11-SC | Tampering | npm/pip installs | accept | No new npm dependencies — all files are static (per STATE.md decision) |
</threat_model>

<verification>
1. grep -c '<url>' public/sitemap.xml — must return 10
2. grep -c 'samsux.co.uk' public/sitemap.xml — must return 10 (every URL on the production domain)
3. grep -v '\.html' public/sitemap.xml | grep -c 'loc' — must return 10 (no .html in any loc)
4. grep -c 'Sitemap:' public/robots.txt — must return 1
5. grep -c 'cleanUrls' vercel.json — must return 1
6. grep -c 'application/ld+json' index.html src/pages/about.html — must return 1 for each
7. npx vite build exits 0
</verification>

<success_criteria>
- index.html and about.html contain JSON-LD Person schema with name, jobTitle, url, email, sameAs (SEO-07)
- public/sitemap.xml lists all 10 pages with clean https://www.samsux.co.uk paths (SEO-08)
- public/robots.txt allows all crawlers and references the sitemap (SEO-09)
- vercel.json contains cleanUrls: true (SEO-10)
- Build passes with zero errors
- SEO-07, SEO-08, SEO-09, SEO-10 satisfied
</success_criteria>

<output>
Create `.planning/phases/11-seo/11-02-SUMMARY.md` when done
</output>
