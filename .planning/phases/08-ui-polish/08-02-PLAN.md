---
phase: 08-ui-polish
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - src/components/nav.html
  - src/styles/3-components/_nav.css
autonomous: true
requirements:
  - UI-05
  - UI-06
  - UI-07
  - UI-08
  - UI-09

must_haves:
  truths:
    - "Nav shows Home as the first link with no designer name on the left"
    - "Nav has no bottom border stroke — it floats above the page"
    - "Hovering a nav link reveals a left-to-right underline animation"
    - "The active nav link shows a persistent underline and semibold weight at 905px+"
    - "Touch/pointer devices see no hover underline (hover gate applied)"
  artifacts:
    - path: "src/components/nav.html"
      provides: "Home li as first nav item; no nav__logo anchor; Work href updated"
      contains: "nav__link{{#if navHome}}"
    - path: "src/styles/3-components/_nav.css"
      provides: "Underline animation via ::after; border-bottom and logo rules removed; active state via scaleX"
      contains: ".nav__link::after"
  key_links:
    - from: "src/components/nav.html"
      to: "src/styles/3-components/_nav.css"
      via: ".nav__link class + position: relative enabling ::after"
      pattern: "nav__link"
    - from: ".nav__link::after"
      to: "@media (hover: hover) and (pointer: fine)"
      via: "hover gate wrapping the :hover transform"
      pattern: "hover: hover"
---

<objective>
Redesign the nav: remove the logo link, add a Home link as the first item, strip the bottom border, and replace the current background-color active/hover approach with a left-to-right underline animation via ::after pseudo-element. Active link gets semibold weight.

Purpose: Nav feels intentional — flat, minimal, with a refined micro-interaction that suits a product designer's portfolio.
Output: Updated nav.html and _nav.css implementing all five nav requirements per the UI-SPEC.
</objective>

<execution_context>
@C:/Users/sambl/OneDrive/Documents/my-portfolio-v2/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/sambl/OneDrive/Documents/my-portfolio-v2/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@C:/Users/sambl/OneDrive/Documents/my-portfolio-v2/.planning/ROADMAP.md
@C:/Users/sambl/OneDrive/Documents/my-portfolio-v2/.planning/phases/08-ui-polish/08-UI-SPEC.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Restructure nav partial HTML</name>
  <files>src/components/nav.html</files>
  <read_first>
    - src/components/nav.html (read the full file before touching anything — note current logo anchor, nav__list order, Work href, and navWork flag comment)
    - .planning/phases/08-ui-polish/08-UI-SPEC.md (Nav partial restructure section — UI-05, UI-06)
  </read_first>
  <action>
    Make these three targeted changes to nav.html:

    1. REMOVE the logo anchor entirely:
       Remove: `<a href="/" class="nav__logo" aria-label="Sam Blake — back to home">Sam Blake</a>`
       This is the element between .nav__inner's opening tag and the hamburger button.

    2. ADD a Home li as the FIRST item in .nav__list, before the existing Work li:
       ```
       <li class="nav__item">
         <a href="/" class="nav__link{{#if navHome}} nav__link--active{{/if}}"{{#if navHome}} aria-current="page"{{/if}}>Home</a>
       </li>
       ```
       The navHome flag enables the active state when set. No existing page sets navHome=true — that is intentional per the spec (homepage uses bare `{{> nav}}` with no flag, which is correct).

    3. UPDATE the Work link href from `/#case-studies` to `/src/pages/work.html`.
       The Work li currently reads: `href="/#case-studies"` — change only the href value, leave all other attributes unchanged.

    Final nav__list order: Home → Work → About → Stories → Get in touch (CTA).
    The comment block at the top of the file: update the Usage section to add `navHome=true — Home page active state (reserved, no existing page sets this)`.
  </action>
  <verify>
    <automated>grep -c "nav__logo" src/components/nav.html</automated>
    Must return 0 (logo anchor removed).
  </verify>
  <acceptance_criteria>
    - nav.html does NOT contain `class="nav__logo"` anywhere
    - nav.html does NOT contain `aria-label="Sam Blake — back to home"` anywhere
    - nav.html contains a `<li class="nav__item">` with `href="/"` as the FIRST li in .nav__list
    - nav.html contains `{{#if navHome}} nav__link--active{{/if}}` on the Home anchor
    - nav.html contains `{{#if navHome}} aria-current="page"{{/if}}` on the Home anchor
    - nav.html contains `href="/src/pages/work.html"` on the Work anchor (not `href="/#case-studies"`)
    - The Home li appears before the Work li in source order
  </acceptance_criteria>
  <done>nav.html has Home as first nav item, no logo anchor, and Work href pointing to /src/pages/work.html.</done>
</task>

<task type="auto">
  <name>Task 2: Update nav CSS — remove border/logo, add underline animation, update active state</name>
  <files>src/styles/3-components/_nav.css</files>
  <read_first>
    - src/styles/3-components/_nav.css (read the full file — pay attention to: .nav border-bottom at line 15; .nav__logo block lines 34–55; .nav__inner margin-right logic; the 905px media query active state at lines 238–241; the prefers-reduced-motion block at the end)
    - .planning/phases/08-ui-polish/08-UI-SPEC.md (Interaction Contracts section — full ::after spec, active state changes, UI-07 border removal, animation timing)
  </read_first>
  <action>
    Make these five targeted changes to _nav.css:

    1. REMOVE the nav border-bottom (UI-07):
       In the `.nav` block, remove: `border-bottom: 1px solid var(--color-border);`
       The `.nav--scrolled` box-shadow is KEPT — do not touch it.

    2. REMOVE all .nav__logo rules (UI-06):
       Delete the entire `.nav__logo` block and `.nav__logo:hover` and `.nav__logo:focus-visible` blocks (currently lines 34–55 approximately). The comment `/* Logo */` above the block should also be removed.

    3. ADD margin-left: auto to .nav__menu at 905px+ to push nav cluster right (replaces the logo's margin-right: auto role):
       Inside the existing `@media (min-width: 905px)` block, in the `.nav__menu` ruleset, add:
       `margin-left: auto;`

    4. UPDATE .nav__link at 905px+ — replace background-color hover and active approach with ::after underline (UI-08, UI-09):

       a. In the base `.nav__link` block (outside media queries), add `position: relative;` to make the element a containing block for ::after. Also update the transition property — keep color transition but remove or replace background-color transition since hover no longer changes background.

       b. In the 905px media query, REMOVE from `.nav__link`:
          - `border-radius: var(--radius-md);`
          - `border-bottom: 2px solid transparent;`

       c. REMOVE the existing hover rule at 905px+ that sets `background-color: var(--color-surface)`:
          Remove from inside `@media (min-width: 905px) { @media (hover: hover) and (pointer: fine) { .nav__link:hover { background-color: ...; } } }`

       d. REMOVE the existing `.nav__link[aria-current="page"]` block at 905px+ that sets `background-color` and `border-bottom`. (The mobile active state at line 179 — `color: var(--color-text-primary); font-weight: var(--font-weight-semibold);` — is KEPT as-is.)

       e. ADD the ::after pseudo-element block INSIDE the 905px media query:
          Selector: `.nav__link::after`
          Properties: content: ''; position: absolute; bottom: 0; left: 0; width: 100%; height: 2px; background-color: var(--color-accent-accessible); transform: scaleX(0); transform-origin: left center;
          Wrap the transition in `@media (prefers-reduced-motion: no-preference)`:
          transition: transform 200ms var(--ease-out);

       f. ADD the hover trigger INSIDE `@media (min-width: 905px)` and INSIDE `@media (hover: hover) and (pointer: fine)`:
          `.nav__link:hover::after { transform: scaleX(1); }`

       g. ADD the active/current underline and semibold weight at 905px+ (UI-08, UI-09):
          `.nav__link[aria-current="page"]::after { transform: scaleX(1); }`
          `.nav__link[aria-current="page"] { font-weight: var(--font-weight-semibold); }`
          These go in the 905px media query (the semibold was already set in the mobile base rule — confirm it is also set at 905px+, or the base rule cascades correctly — the base `.nav__link[aria-current="page"]` block sets it so it cascades fine).

    5. ADD reduced-motion guard in the existing `@media (prefers-reduced-motion: reduce)` block at the bottom of the file:
       `.nav__link::after { transition: none; }`
  </action>
  <verify>
    <automated>grep -c "nav__link::after" src/styles/3-components/_nav.css</automated>
    Must return 3 or more (the main ::after block, the :hover::after, and the aria-current::after).
  </verify>
  <acceptance_criteria>
    - _nav.css does NOT contain `border-bottom: 1px solid var(--color-border)` in the `.nav` block
    - _nav.css does NOT contain `.nav__logo` anywhere (all three blocks removed)
    - _nav.css contains `.nav__link::after` with `transform: scaleX(0)` and `transform-origin: left center`
    - _nav.css contains `.nav__link:hover::after { transform: scaleX(1); }` inside `@media (hover: hover) and (pointer: fine)`
    - _nav.css contains `.nav__link[aria-current="page"]::after { transform: scaleX(1); }` inside the 905px media query
    - _nav.css contains `background-color: var(--color-accent-accessible)` in the `.nav__link::after` block
    - _nav.css does NOT contain `background-color: var(--color-surface)` in any nav__link hover rule at 905px+
    - _nav.css does NOT contain `background-color: var(--color-surface)` in any aria-current rule at 905px+
    - _nav.css contains `margin-left: auto` on `.nav__menu` inside the 905px media query
    - _nav.css contains `.nav__link::after { transition: none; }` inside `@media (prefers-reduced-motion: reduce)`
    - _nav.css contains `position: relative` on `.nav__link` (base block, outside media queries)
  </acceptance_criteria>
  <done>_nav.css has no logo rules, no nav border-bottom, no background-color hover/active; underline animation via ::after with hover gate and reduced-motion guard; active link shows persistent underline and semibold weight at 905px+.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| No new boundaries | Nav is a static Handlebars partial; no user input; no external services |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-08B-01 | Tampering | nav.html Handlebars flags | accept | Flag values are build-time constants set per-page, not user-supplied |
| T-08B-SC | Tampering | npm/pip/cargo installs | accept | No package installs in this plan — vanilla CSS/HTML edits only |
</threat_model>

<verification>
After both tasks complete:
- Run `npm run dev` and visit http://localhost:5173 — nav shows: Home | Work | About | Stories | Get in touch
- No designer name on the left side; the nav menu is pushed to the right
- Nav has no visible bottom border stroke (just floats)
- Hover any text nav link with mouse: underline slides in left-to-right over 200ms in accent green
- On mobile (< 905px): no underline animation on tap (hover gate active)
- Check About page: About link is bold with visible underline; other links have no underline
- `prefers-reduced-motion: reduce` in DevTools: underline appears instantly with no transition
</verification>

<success_criteria>
Nav shows Home as first link, no logo, no bottom border, hover underline animates left-to-right (mouse only), and the active page link has a persistent underline and semibold weight on desktop.
</success_criteria>

<output>
Create `.planning/phases/08-ui-polish/08-02-SUMMARY.md` when done.
</output>
