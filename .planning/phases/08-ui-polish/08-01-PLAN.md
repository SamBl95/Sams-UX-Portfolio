---
phase: 08-ui-polish
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - index.html
  - src/styles/3-components/_hero.css
autonomous: true
requirements:
  - UI-01
  - UI-02
  - UI-03

must_haves:
  truths:
    - "Hero displays 'Sam Blake' as an accented h2 above the h1 typewriter heading"
    - "The h1 text reads 'A Product Designer who' followed by the typewriter"
    - "The subheadline paragraph (one-liner) is gone from the hero"
    - "Hero content fills available width at 1440px — no max-width constraint"
  artifacts:
    - path: "index.html"
      provides: "Updated hero HTML with h2.hero__name and revised h1 text, no subheadline"
      contains: "hero__name"
    - path: "src/styles/3-components/_hero.css"
      provides: "hero__name styles; stagger reassignment; 1440px max-width removal"
      contains: ".hero__name"
  key_links:
    - from: "index.html"
      to: "src/styles/3-components/_hero.css"
      via: "class hero__name on h2 element"
      pattern: "class=\"hero__name\""
---

<objective>
Restructure the hero section: replace the eyebrow paragraph with an accented "Sam Blake" h2, update the h1 text to start with "A Product Designer who", remove the subheadline one-liner, and strip the 1440px max-width constraint from hero content.

Purpose: Makes the hero feel intentional — the name is prominent, the headline is a statement, and the layout breathes at all widths.
Output: Modified index.html and _hero.css with the correct hero structure per the UI-SPEC.
</objective>

<execution_context>
@C:/Users/sambl/OneDrive/Documents/my-portfolio-v2/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/sambl/OneDrive/Documents/my-portfolio-v2/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@C:/Users/sambl/OneDrive/Documents/my-portfolio-v2/.planning/ROADMAP.md
@C:/Users/sambl/OneDrive/Documents/my-portfolio-v2/.planning/phases/08-ui-polish/08-UI-SPEC.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Restructure hero HTML</name>
  <files>index.html</files>
  <read_first>
    - index.html (read current hero markup before touching anything)
    - .planning/phases/08-ui-polish/08-UI-SPEC.md (Structural Changes Contract section — hero restructure)
  </read_first>
  <action>
    In the .hero__content div of index.html, make these three targeted changes:

    1. REMOVE the eyebrow paragraph entirely:
       Remove: `<p class="hero__eyebrow">Product Designer</p>`

    2. REPLACE with an h2 hero name above the h1. Insert before the h1:
       `<h2 class="hero__name">Sam Blake</h2>`

    3. CHANGE the h1 opening text from "The designer who" to "A Product Designer who". The typewriter span and screen-reader span inside the h1 are unchanged — only the leading text node changes.

    4. REMOVE the subheadline paragraph entirely:
       Remove: `<p class="hero__subheadline">...</p>` (the entire element including its content)

    The resulting element order inside .hero__content must be:
    h2.hero__name → h1.hero__headline → div.hero__actions

    The typewriter span remains inside the h1. The hero__actions div is unchanged.
  </action>
  <verify>
    <automated>grep -c "hero__name" index.html</automated>
    The command must return 1 (the h2 element exists).
    Additionally confirm: grep returns 0 for "hero__eyebrow" and 0 for "hero__subheadline" in index.html.
    Confirm h1 text reads: "A Product Designer who"
  </verify>
  <acceptance_criteria>
    - index.html contains `class="hero__name"` on an h2 element
    - index.html contains the text "Sam Blake" inside that h2
    - index.html contains the text "A Product Designer who" as the leading text node of the h1.hero__headline
    - index.html does NOT contain "hero__eyebrow"
    - index.html does NOT contain "hero__subheadline"
    - The typewriter markup (hero__typewriter, hero__typewriter-text, hero__typewriter-sr) remains inside the h1 unchanged
    - The hero__actions div remains unchanged with both CTA buttons
  </acceptance_criteria>
  <done>Hero HTML has three elements inside .hero__content: h2.hero__name, h1.hero__headline (with typewriter), div.hero__actions. No eyebrow. No subheadline.</done>
</task>

<task type="auto">
  <name>Task 2: Update hero CSS — name styles, stagger, width</name>
  <files>src/styles/3-components/_hero.css</files>
  <read_first>
    - src/styles/3-components/_hero.css (read the full file before touching anything — particularly the stagger selector list, .hero__eyebrow, .hero__subheadline, and the 1440px media query)
    - .planning/phases/08-ui-polish/08-UI-SPEC.md (CSS for .hero__name block; hero content width section; stagger order)
  </read_first>
  <action>
    Make these four targeted changes to _hero.css:

    1. ADD .hero__name CSS block after the @keyframes hero-enter block (before or after the existing .hero__content block — either is fine as long as it is within the components layer). The exact ruleset from the UI-SPEC:
       Selector: .hero__name
       Properties: font-family: var(--font-heading); font-weight: var(--font-weight-semibold); font-optical-sizing: auto; color: var(--color-accent-accessible); letter-spacing: -0.02em; margin-bottom: var(--space-3); line-height: var(--leading-tight);
       No font-size rule — the element-level h2 size from _typography.css applies.

    2. UPDATE the stagger animation selector list. The current list is:
       `.hero__eyebrow, .hero__headline, .hero__typewriter, .hero__subheadline, .hero__actions`
       Replace with:
       `.hero__name, .hero__headline, .hero__typewriter, .hero__actions`
       (eyebrow removed, subheadline removed, name added as first item)

    3. UPDATE the animation-delay assignments. New mapping:
       .hero__name:       animation-delay: 0ms;
       .hero__headline:   animation-delay: 80ms;
       .hero__typewriter: animation-delay: 160ms;
       .hero__actions:    animation-delay: 240ms;
       Remove the .hero__eyebrow and .hero__subheadline delay rules.

    4. REMOVE the max-width override inside @media (min-width: 1440px). The rule to remove is:
       `max-width: var(--hero-content-max-width);`
       from the .hero__content block inside that media query. The `margin-inline: 0` comment-and-rule in the same block can also be removed (without max-width the margin-inline is a no-op). Leave the --hero-content-max-width token in _variables.css untouched.
  </action>
  <verify>
    <automated>grep -c "hero__name" src/styles/3-components/_hero.css</automated>
    Must return 2 or more (the selector in the animation list + the standalone block).
  </verify>
  <acceptance_criteria>
    - _hero.css contains a `.hero__name` ruleset with `color: var(--color-accent-accessible)`
    - _hero.css contains `.hero__name` in the animation selector list (alongside .hero__headline, .hero__typewriter, .hero__actions)
    - _hero.css does NOT contain `.hero__eyebrow` (selector removed from stagger list and any standalone block)
    - _hero.css does NOT contain `.hero__subheadline` anywhere
    - _hero.css does NOT contain `max-width: var(--hero-content-max-width)` anywhere
    - `.hero__name` delay is `0ms`, `.hero__actions` delay is `240ms`
    - `font-family: var(--font-heading)` is present in the `.hero__name` block
    - `letter-spacing: -0.02em` is present in the `.hero__name` block
  </acceptance_criteria>
  <done>_hero.css has .hero__name styles with accent color and Fraunces font; stagger list updated to four elements; 1440px max-width constraint removed.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| No new boundaries | Phase is pure static HTML/CSS edits with no user input, no external services, no JS changes |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-08A-01 | Tampering | index.html hero markup | accept | Static HTML file, no server-side rendering; no user-supplied content in this section |
| T-08A-SC | Tampering | npm/pip/cargo installs | accept | No package installs in this plan — vanilla CSS/HTML edits only |
</threat_model>

<verification>
After both tasks complete:
- Run `npm run dev` and visit http://localhost:5173 — hero shows "Sam Blake" in accent green above the typewriter h1
- h1 reads "A Product Designer who [typewriter]" with no subheadline paragraph below
- At 1440px viewport width the hero text fills the full container width
- Check DevTools: .hero__name has color: #1a6b52 (--color-accent-accessible), font-family Fraunces, letter-spacing -0.02em
- Entrance animation: name fades in first, then headline at 80ms, typewriter at 160ms, buttons at 240ms
</verification>

<success_criteria>
Hero section shows h2 "Sam Blake" in accent color (--color-accent-accessible) above h1 "A Product Designer who [typewriter]" with no subheadline beneath it, and at 1440px+ the content uses the full container width.
</success_criteria>

<output>
Create `.planning/phases/08-ui-polish/08-01-SUMMARY.md` when done.
</output>
