---
phase: 08-ui-polish
plan: 03
type: execute
wave: 2
depends_on:
  - "08-01"
  - "08-02"
files_modified:
  - src/styles/3-components/_about.css
  - src/styles/3-components/_case-study.css
  - src/styles/3-components/_stories.css
  - src/pages/work.html
  - src/styles/3-components/_work.css
  - src/styles/main.css
  - vite.config.js
autonomous: true
requirements:
  - UI-04
  - UI-10
  - UI-11

must_haves:
  truths:
    - "Paragraph text on About, case study, and Stories pages fills available width — no 72ch constraint"
    - "A /work page exists with hero, three feature rows, and is reachable from the Work nav link"
    - "Each feature row shows an image placeholder left (40%) and copy column right (60%) on desktop"
    - "Work page is registered in vite.config.js and _work.css is imported in main.css"
  artifacts:
    - path: "src/pages/work.html"
      provides: "Work page with canonical scaffold, navWork=true, three work__feature articles"
      contains: "work__feature"
    - path: "src/styles/3-components/_work.css"
      provides: "Feature section layout: 2-col grid 905px+, image placeholder, copy column"
      contains: ".work__feature"
    - path: "src/styles/main.css"
      provides: "_work.css imported after _case-studies.css"
      contains: "_work.css"
    - path: "vite.config.js"
      provides: "work page registered under key 'work'"
      contains: "work: r('./src/pages/work.html')"
  key_links:
    - from: "src/components/nav.html"
      to: "src/pages/work.html"
      via: "Work link href /src/pages/work.html set in Plan 02"
      pattern: "work.html"
    - from: "src/pages/work.html"
      to: "src/styles/3-components/_work.css"
      via: "BEM classes work__feature, work__heading consumed by _work.css"
      pattern: "work__feature"
---

<objective>
Three tasks in this Wave 2 plan: (1) remove 72ch max-width from paragraph selectors in About, case study, and Stories CSS; (2) create the /work page with hero heading and three feature-section articles; (3) create _work.css with the feature layout, register the page in main.css and vite.config.js.

Purpose: Content reads at natural width; a dedicated Work page anchors the nav and gives case studies a feature-level showcase.
Output: Three CSS files patched, one new HTML page, one new CSS file, main.css and vite.config.js updated.
</objective>

<execution_context>
@C:/Users/sambl/OneDrive/Documents/my-portfolio-v2/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/sambl/OneDrive/Documents/my-portfolio-v2/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@C:/Users/sambl/OneDrive/Documents/my-portfolio-v2/.planning/ROADMAP.md
@C:/Users/sambl/OneDrive/Documents/my-portfolio-v2/.planning/phases/08-ui-polish/08-UI-SPEC.md
@C:/Users/sambl/OneDrive/Documents/my-portfolio-v2/.planning/phases/08-ui-polish/08-01-SUMMARY.md
@C:/Users/sambl/OneDrive/Documents/my-portfolio-v2/.planning/phases/08-ui-polish/08-02-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Remove 72ch max-width from paragraph selectors</name>
  <files>
    src/styles/3-components/_about.css,
    src/styles/3-components/_case-study.css,
    src/styles/3-components/_stories.css
  </files>
  <read_first>
    - src/styles/3-components/_about.css (the .about-section__body p block at line 70–77 — confirms max-width: 72ch is at line 75)
    - src/styles/3-components/_case-study.css (the .cs-phase__body p block at lines 278–284 — confirms max-width: 72ch is at line 283)
    - src/styles/3-components/_stories.css (the .post__body p block at lines 121–123 — confirms max-width: 72ch is at line 122)
    - .planning/phases/08-ui-polish/08-UI-SPEC.md (Paragraph width removal section — confirms which selectors and which to NOT touch)
  </read_first>
  <action>
    Make one targeted removal in each of the three files:

    FILE 1 — src/styles/3-components/_about.css:
    In the `.about-section__body p` ruleset, remove only: `max-width: 72ch;`
    All other properties in that block (font-size, line-height, color, margin-bottom) are KEPT.

    FILE 2 — src/styles/3-components/_case-study.css:
    In the `.cs-phase__body p` ruleset, remove only: `max-width: 72ch;`
    All other properties in that block (font-size, line-height, color, margin-bottom) are KEPT.

    FILE 3 — src/styles/3-components/_stories.css:
    In the `.post__body p` ruleset, remove only: `max-width: 72ch;`
    All other properties in that block (margin-top from the adjacent .post__body p + p selector) are KEPT.

    Do NOT touch:
    - .card__subtitle (24ch) — intentional card layout constraint
    - .stat-block or .metrics-row label constraints — intentional chip sizing
    - Any other max-width not listed above
  </action>
  <verify>
    <automated>grep -rn "max-width.*72ch" src/styles/3-components/</automated>
    Must return zero matches — all three 72ch constraints removed.
  </verify>
  <acceptance_criteria>
    - _about.css does NOT contain `max-width: 72ch` (or `max-width:     72ch` with any whitespace)
    - _case-study.css does NOT contain `max-width: 72ch`
    - _stories.css does NOT contain `max-width: 72ch`
    - _about.css still contains `.about-section__body p` with `font-size`, `line-height`, `color`, `margin-bottom` intact
    - _case-study.css still contains `.cs-phase__body p` with `font-size`, `line-height`, `color`, `margin-bottom` intact
    - _stories.css still contains `.post__body p` block (only max-width line removed)
    - No other max-width values anywhere in the three files are changed
  </acceptance_criteria>
  <done>72ch max-width removed from .about-section__body p, .cs-phase__body p, and .post__body p. All surrounding properties unchanged.</done>
</task>

<task type="auto">
  <name>Task 2: Create work.html and _work.css; register in main.css and vite.config.js</name>
  <files>
    src/pages/work.html,
    src/styles/3-components/_work.css,
    src/styles/main.css,
    vite.config.js
  </files>
  <read_first>
    - vite.config.js (read before editing — confirm current input keys and the `r()` helper pattern)
    - src/styles/main.css (read the components import block — confirm _case-studies.css line to insert after)
    - .planning/phases/08-ui-polish/08-UI-SPEC.md (Work page section — full page structure, feature layout spec, copywriting contract, title/meta description)
    - CLAUDE.md (canonical page scaffold — use this as the exact HTML template including head, fonts, nav flag, footer, scripts)
    - src/styles/3-components/_card.css (skim for .card__metric-chip BEM structure to reuse in feature copy column)
  </read_first>
  <action>
    Create four artifacts:

    --- src/pages/work.html ---
    Follow the canonical page scaffold from CLAUDE.md exactly. Key values:
    - title: `Work — Sam Blake | Product Designer`
    - meta description: `Three product design case studies from Santander UK — delivering measurable outcomes across search, AI tooling, and community support.` (149 chars)
    - nav partial: `{{> nav navWork=true}}`
    - No script tags other than theme.js (this page has no typewriter or reveal animations)

    Page body structure (two sections):

    Section 1 — hero:
    `<section class="work section-lg" aria-labelledby="work-heading">`
    `<div class="container">`
    `<h1 id="work-heading" class="work__heading">Selected work</h1>`
    `<p class="work__subheading">Three case studies from Santander UK</p>`
    `</div>`

    Section 2 — feature cases:
    `<section class="work__cases section-lg">`
    `<div class="container">`
    Three `<article class="work__feature">` elements. Each article contains:
    - `<div class="work__feature-image" aria-hidden="true"></div>` (image placeholder — styled by CSS)
    - `<div class="work__feature-copy">` containing:
      - `<div class="card__metric-chip"><span class="card__metric-value">[metric]</span><span class="card__metric-label">[label]</span></div>`
      - `<h2 class="work__feature-title">[case study title]</h2>`
      - `<p class="work__feature-desc">[short description]</p>`
      - `<a href="[case-study-url]" class="btn btn--ghost">Read case study <span aria-hidden="true">→</span></a>`

    Case study data (from existing cards in index.html):
    Feature 1: metric="35%" label="Search accuracy improvement" title="i-Exchange Knowledge Base Overhaul" desc="Overhauling Santander's 6,000-article internal knowledge base to fix broken search — delivering a 35% accuracy improvement across 5 million annual searches." href="/src/pages/case-studies/i-exchange.html"
    Feature 2: metric="30%" label="Increase in feedback submissions" title="Cassi — AI Chatbot Feedback Redesign" desc="Fixing Santander's AI colleague assistant in three weeks at zero development cost — increasing feedback submission rates by 30%." href="/src/pages/case-studies/cassi.html"
    Feature 3: metric="97 hrs" label="Saved every month" title="Community Support Forum Redesign" desc="Halving homepage load times and saving 97 hours a month by scaling Santander's internal community forum — without a complete rebuild." href="/src/pages/case-studies/community.html"

    --- src/styles/3-components/_work.css ---
    Create a new CSS file with these rules. No hex values — tokens only.

    Section header:
    `.work__heading` — font-weight: var(--font-weight-semibold); letter-spacing: -0.02em; color: var(--color-text-primary); margin-bottom: var(--space-3); (font-size from h1 element rule)
    `.work__subheading` — font-size: var(--text-lg); color: var(--color-text-secondary); line-height: var(--leading-body); margin-bottom: 0;

    Feature article — mobile: single column, stacked:
    `.work__feature` — display: flex; flex-direction: column; gap: var(--space-6);
    `.work__feature + .work__feature` — margin-top: var(--space-12); (96px gap between rows)

    Image placeholder:
    `.work__feature-image` — background-color: var(--color-surface); border-radius: var(--radius-lg); aspect-ratio: 16 / 9; width: 100%;

    Copy column:
    `.work__feature-copy` — display: flex; flex-direction: column; gap: var(--space-4);
    `.work__feature-title` — font-family: var(--font-heading); font-weight: var(--font-weight-semibold); font-optical-sizing: auto; letter-spacing: -0.015em; color: var(--color-text-primary); line-height: var(--leading-tight); font-size: var(--text-2xl);
    `.work__feature-desc` — font-size: var(--text-base); line-height: var(--leading-body); color: var(--color-text-secondary);

    Desktop 905px+ — two-column grid:
    `@media (min-width: 905px)`:
    `.work__feature` — flex-direction: row; align-items: flex-start; gap: var(--space-8);
    `.work__feature-image` — flex: 0 0 40%; (image column is 40%)
    `.work__feature-copy` — flex: 1; (copy column fills remaining 60%)
    `.work__feature-title` — font-size: var(--text-3xl);

    --- src/styles/main.css ---
    In the `/* 3. COMPONENTS */` block, add after the `_case-studies.css` import line:
    `@import './3-components/_work.css';`

    --- vite.config.js ---
    In the `rollupOptions.input` object, add after the `storiesPost` entry:
    `work: r('./src/pages/work.html'),`
  </action>
  <verify>
    <automated>grep -c 'class="work__feature"' src/pages/work.html</automated>
    Must return 3 (one per article element — matches only the article wrapper, not child variants).
    Also verify: grep -c "_work.css" src/styles/main.css returns 1.
    Also verify: grep -c "work.*work.html" vite.config.js returns 1.
  </verify>
  <acceptance_criteria>
    - src/pages/work.html exists and contains `<title>Work — Sam Blake | Product Designer</title>`
    - src/pages/work.html contains `{{> nav navWork=true}}`
    - src/pages/work.html contains exactly three `<article class="work__feature">` elements
    - src/pages/work.html contains `<h1 id="work-heading" class="work__heading">Selected work</h1>`
    - src/pages/work.html contains `<p class="work__subheading">Three case studies from Santander UK</p>`
    - src/pages/work.html contains three `class="work__feature-image"` divs (one per article)
    - src/pages/work.html contains `<script type="module" src="/src/theme.js"></script>`
    - src/styles/3-components/_work.css exists and contains `.work__feature-image` with `aspect-ratio: 16 / 9`
    - src/styles/3-components/_work.css contains `.work__feature-title` with `font-family: var(--font-heading)`
    - src/styles/3-components/_work.css contains `@media (min-width: 905px)` block with `.work__feature { flex-direction: row`
    - src/styles/3-components/_work.css contains `flex: 0 0 40%` on `.work__feature-image` at 905px+
    - src/styles/main.css contains `@import './3-components/_work.css'` after the `_case-studies.css` import
    - vite.config.js contains `work: r('./src/pages/work.html')`
    - No hex values in _work.css — all colors are CSS custom property references
  </acceptance_criteria>
  <done>work.html created with three feature articles; _work.css created with 2-col layout at 905px+; main.css imports _work.css; vite.config.js registers the work page.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| No new boundaries | Static HTML/CSS page creation; no user input; no external services or APIs |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-08C-01 | Information Disclosure | work.html meta description | accept | Public page; description is intentionally public marketing copy |
| T-08C-02 | Tampering | vite.config.js input registration | accept | Build config is not user-facing; no runtime code injection risk |
| T-08C-SC | Tampering | npm/pip/cargo installs | accept | No package installs in this plan — new HTML/CSS files and config edits only |
</threat_model>

<verification>
After both tasks complete:
- Run `npm run dev`
- Visit http://localhost:5173/src/pages/work.html — page loads with "Selected work" h1 and three feature rows
- On desktop (905px+): each row shows image placeholder on the left (~40%) and copy on the right — two-column layout
- On mobile: each row stacks vertically (placeholder above, copy below)
- Image placeholders are surface-colored rectangles with 16:9 aspect ratio and rounded corners
- Each feature row has a metric chip, case study title in Fraunces, description paragraph, and "Read case study →" ghost button
- Clicking "Work" in the nav navigates to the work page
- Visit About page or Stories — paragraph text fills full container width (no 72ch constraint visible)
- Visit case study pages — body paragraphs fill full width
- Run `npm run build` — no errors; work page included in dist output
</verification>

<success_criteria>
/work page exists with three feature rows (2-col at 905px+), is reachable from the Work nav link, _work.css is imported in correct ITCSS order, vite.config.js registers the page, and 72ch paragraph constraints are removed from About/case study/Stories CSS.
</success_criteria>

<output>
Create `.planning/phases/08-ui-polish/08-03-SUMMARY.md` when done.
</output>
