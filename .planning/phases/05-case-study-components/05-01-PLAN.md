---
phase: 05-case-study-components
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/styles/3-components/_image-block.css
  - src/styles/3-components/_callout.css
  - src/styles/3-components/_pull-quote.css
  - src/styles/main.css
autonomous: true
requirements:
  - CS-01
  - CS-02
  - CS-03
  - CS-08

must_haves:
  truths:
    - "An image inside .image-block--full renders full-width within its container"
    - "An image inside .image-block--aside floats right at 40% width at 905px+ and stacks on mobile"
    - ".callout--insight renders with --color-accent-light background and 3px teal left border"
    - ".callout--warning renders with --color-surface background and 3px --color-border left border"
    - ".callout--quote renders transparent with italic body text"
    - ".pull-quote__text renders in Fraunces at --text-3xl mobile and --text-4xl at 905px+"
    - "Pull-quote has a 3px teal left accent bar via ::before pseudo-element"
    - "All 3 new CSS files are imported in main.css after _reveal.css"
  artifacts:
    - path: "src/styles/3-components/_image-block.css"
      provides: "Three layout variants: full-bleed, contained, aside float"
      contains: ".image-block--aside"
    - path: "src/styles/3-components/_callout.css"
      provides: "Three callout variants: insight, warning, quote"
      contains: ".callout--insight"
    - path: "src/styles/3-components/_pull-quote.css"
      provides: "Display pull-quote with Fraunces type and accent bar"
      contains: ".pull-quote__text"
  key_links:
    - from: "src/styles/main.css"
      to: "src/styles/3-components/_image-block.css"
      via: "@import statement after _reveal.css"
      pattern: "@import.*_image-block\\.css"
    - from: "src/styles/main.css"
      to: "src/styles/3-components/_callout.css"
      via: "@import statement"
      pattern: "@import.*_callout\\.css"
    - from: "src/styles/main.css"
      to: "src/styles/3-components/_pull-quote.css"
      via: "@import statement"
      pattern: "@import.*_pull-quote\\.css"
---

<objective>
Create three content-component CSS files for case study pages: image-block (figure with layout variants), callout (aside with three variants), and pull-quote (blockquote with Fraunces display type). Wire all three into main.css.

Purpose: Case study pages need structured storytelling components so content authors can embed images, callouts, and pull-quotes without writing bespoke CSS.
Output: Three new CSS files under src/styles/3-components/ plus three new @import lines in main.css.
</objective>

<execution_context>
@C:/Users/sambl/OneDrive/Documents/my-portfolio-v2/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/sambl/OneDrive/Documents/my-portfolio-v2/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@C:/Users/sambl/OneDrive/Documents/my-portfolio-v2/.planning/ROADMAP.md
@C:/Users/sambl/OneDrive/Documents/my-portfolio-v2/.planning/phases/05-case-study-components/05-UI-SPEC.md

<interfaces>
<!-- Established patterns the executor must replicate. Extracted from codebase. -->

From src/styles/3-components/_case-study.css (existing callout pattern and focus outline):
```css
/* Existing callout — executor extends this pattern in _callout.css */
.cs-callout { … }

/* Focus outline pattern used project-wide */
:focus-visible {
  outline: 2px solid var(--color-accent-accessible);
  outline-offset: 2px;
  border-radius: var(--radius-sm);
}
```

From src/styles/main.css (current last component import — new imports go after this):
```css
@import './3-components/_reveal.css';
```

From src/styles/1-settings/_variables.css (radius tokens):
```css
--radius-sm: 4px;
--radius-md: 6px;
--radius-lg: 8px;
--radius-xl: 12px;
--radius-full: 9999px;
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create _image-block.css</name>
  <files>src/styles/3-components/_image-block.css</files>
  <read_first>
    - src/styles/3-components/_case-study.css (established BEM pattern and naming conventions)
    - .planning/phases/05-case-study-components/05-UI-SPEC.md (CS-01 section)
    - src/styles/1-settings/_variables.css (radius and spacing tokens)
  </read_first>
  <action>
    Create src/styles/3-components/_image-block.css.

    Base figure element (.image-block):
    - list-style: none; margin: 0; padding: 0 (figure element reset)
    - display: block

    Contained variant (.image-block--contained):
    - margin-block: var(--space-8) on mobile
    - At 905px+: margin-block: var(--space-10)
    - img inside: display: block; width: 100%; height: auto; border-radius: var(--radius-lg)

    Full variant (.image-block--full):
    - width: 100%; margin-block: var(--space-12)
    - img inside: display: block; width: 100%; height: auto; border-radius: 0

    Aside variant (.image-block--aside):
    - Mobile (default): same as contained — display block, margin-block: var(--space-8), img full width with radius-lg
    - At 905px+: float: right; width: 40%; margin-left: var(--space-8); margin-block: var(--space-2)
    - Clearfix on .image-block--aside::after: content: ''; display: table; clear: both

    Caption (.image-block__caption):
    - display: block; margin-top: var(--space-2)
    - font-size: var(--text-sm); font-style: italic; line-height: var(--leading-relaxed)
    - color: var(--color-text-secondary)

    No hover, no transition — static display component.
  </action>
  <verify>
    <automated>grep -c "image-block--aside" "C:/Users/sambl/OneDrive/Documents/my-portfolio-v2/src/styles/3-components/_image-block.css"</automated>
  </verify>
  <acceptance_criteria>
    - _image-block.css exists at src/styles/3-components/_image-block.css
    - File contains .image-block--full with border-radius: 0 on img
    - File contains .image-block--aside with float: right and width: 40% inside a 905px media query
    - File contains .image-block--aside::after clearfix (content: ''; display: table; clear: both)
    - File contains .image-block__caption with font-style: italic and color: var(--color-text-secondary)
    - No hex values present — all colors via tokens
  </acceptance_criteria>
  <done>_image-block.css created with three variants (full, contained, aside) and caption styles matching the CS-01 spec.</done>
</task>

<task type="auto">
  <name>Task 2: Create _callout.css and _pull-quote.css</name>
  <files>
    src/styles/3-components/_callout.css
    src/styles/3-components/_pull-quote.css
  </files>
  <read_first>
    - src/styles/3-components/_case-study.css (existing .cs-callout pattern — callout is an independent reusable extension of this)
    - .planning/phases/05-case-study-components/05-UI-SPEC.md (CS-02 and CS-03 sections)
    - src/styles/1-settings/_variables.css (spacing, radius, and color tokens)
  </read_first>
  <action>
    Create src/styles/3-components/_callout.css:

    Base .callout (shared across all variants):
    - padding: var(--space-5) var(--space-6) (40px 48px)
    - margin-block: var(--space-8)
    - border-radius: 0 var(--radius-md) var(--radius-md) 0
    - border-left: 3px solid (color set per variant)

    Insight variant (.callout--insight):
    - background-color: var(--color-accent-light)
    - border-left-color: var(--color-accent)

    Warning variant (.callout--warning):
    - background-color: var(--color-surface)
    - border-left-color: var(--color-border)

    Quote variant (.callout--quote) — use on blockquote element:
    - background-color: transparent
    - border-left-color: var(--color-border)
    - .callout__body inside: font-style: italic

    Eyebrow (.callout__eyebrow) — only visually relevant in insight variant:
    - font-size: var(--text-xs); font-weight: var(--font-weight-semibold)
    - letter-spacing: 0.06em; text-transform: uppercase
    - color: var(--color-accent-accessible); margin-bottom: var(--space-2)

    Body (.callout__body):
    - font-size: var(--text-base); line-height: var(--leading-relaxed)
    - color: var(--color-text-primary); margin: 0

    Cite (.callout__cite) — quote variant only:
    - display: block; font-size: var(--text-sm); color: var(--color-text-secondary)
    - font-style: normal; margin-top: var(--space-3)

    ---

    Create src/styles/3-components/_pull-quote.css:

    .pull-quote (blockquote element):
    - position: relative; padding-left: var(--space-6)
    - margin-block: var(--space-10); border: none (override browser default)

    Left accent bar via .pull-quote::before:
    - content: ''; position: absolute; left: 0; top: 0
    - width: 3px; height: 100%
    - background-color: var(--color-accent)

    Text (.pull-quote__text):
    - font-family: var(--font-heading); font-size: var(--text-3xl)
    - font-weight: var(--font-weight-semibold); line-height: var(--leading-tight)
    - letter-spacing: -0.02em; color: var(--color-text-primary); margin: 0
    - At 905px+: font-size: var(--text-4xl)

    Cite (.pull-quote__cite):
    - display: block; margin-top: var(--space-3)
    - font-size: var(--text-sm); font-weight: var(--font-weight-medium)
    - font-style: normal; color: var(--color-text-secondary)
  </action>
  <verify>
    <automated>grep -c "callout--insight" "C:/Users/sambl/OneDrive/Documents/my-portfolio-v2/src/styles/3-components/_callout.css" && grep -c "pull-quote::before" "C:/Users/sambl/OneDrive/Documents/my-portfolio-v2/src/styles/3-components/_pull-quote.css"</automated>
  </verify>
  <acceptance_criteria>
    - _callout.css contains .callout--insight with background-color: var(--color-accent-light) and border-left-color: var(--color-accent)
    - _callout.css contains .callout--warning with background-color: var(--color-surface) and border-left-color: var(--color-border)
    - _callout.css contains .callout--quote with background-color: transparent and .callout__body font-style: italic
    - _callout.css contains .callout__eyebrow with letter-spacing: 0.06em, text-transform: uppercase, color: var(--color-accent-accessible)
    - _callout.css contains .callout__cite with display: block and font-style: normal
    - _pull-quote.css contains .pull-quote::before with position: absolute, left: 0, top: 0, width: 3px, background-color: var(--color-accent)
    - _pull-quote.css contains @media (min-width: 905px) block setting .pull-quote__text font-size to var(--text-4xl)
    - No hex values in either file
  </acceptance_criteria>
  <done>_callout.css with three variants and _pull-quote.css with Fraunces display text and accent bar created, matching CS-02 and CS-03 specs.</done>
</task>

<task type="auto">
  <name>Task 3: Wire imports into main.css</name>
  <files>src/styles/main.css</files>
  <read_first>
    - src/styles/main.css (current state — new imports go after _reveal.css in the 3. COMPONENTS block)
    - .planning/phases/05-case-study-components/05-UI-SPEC.md (CS-08 section — exact import order)
  </read_first>
  <action>
    Add three @import lines to the /* 3. COMPONENTS */ block in main.css, immediately after the existing @import './3-components/_reveal.css'; line.

    Insert in this exact order:
    1. @import './3-components/_image-block.css';
    2. @import './3-components/_callout.css';
    3. @import './3-components/_pull-quote.css';

    Do not modify any existing import lines. Do not reorder existing imports. The 4. LAYOUTS block remains unchanged and below.

    Note: The CS-08 spec lists all 7 component imports together. Plan 02 will add the remaining 4 (_process-steps, _before-after, _metrics-row, _two-column). This task adds only the 3 components from this plan.
  </action>
  <verify>
    <automated>grep -c "_image-block\|_callout\|_pull-quote" "C:/Users/sambl/OneDrive/Documents/my-portfolio-v2/src/styles/main.css"</automated>
  </verify>
  <acceptance_criteria>
    - main.css contains @import './3-components/_image-block.css';
    - main.css contains @import './3-components/_callout.css';
    - main.css contains @import './3-components/_pull-quote.css';
    - All three new imports appear after @import './3-components/_reveal.css';
    - No existing imports were removed or reordered
    - The /* 4. LAYOUTS */ block is still present and unchanged
  </acceptance_criteria>
  <done>main.css contains three new @import statements for the content components, positioned after _reveal.css in the COMPONENTS block.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| CSS file authoring | All files are authored CSS — no user input, no dynamic content, no external requests |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05a-01 | Information Disclosure | _callout.css — role="note" on aside | accept | ARIA role is intentional accessibility annotation; no PII exposed |
| T-05a-SC | Tampering | npm/pip installs | accept | No package installs in this plan — pure CSS authoring |
</threat_model>

<verification>
After both tasks complete:

1. All three CSS files exist:
   - src/styles/3-components/_image-block.css
   - src/styles/3-components/_callout.css
   - src/styles/3-components/_pull-quote.css

2. main.css import check:
   grep -c "_image-block\|_callout\|_pull-quote" src/styles/main.css
   Expected: 3

3. No hex values in new files:
   grep -rn "#[0-9a-fA-F]\{3,6\}" src/styles/3-components/_image-block.css src/styles/3-components/_callout.css src/styles/3-components/_pull-quote.css
   Expected: 0 matches

4. Variant coverage:
   grep "image-block--aside\|image-block--full\|image-block--contained" src/styles/3-components/_image-block.css
   Expected: all three present

   grep "callout--insight\|callout--warning\|callout--quote" src/styles/3-components/_callout.css
   Expected: all three present
</verification>

<success_criteria>
- _image-block.css: three variants (full, contained, aside) with aside float at 905px+, clearfix, and caption styles
- _callout.css: three variants (insight/warning/quote) differentiated by background and border-left color, eyebrow and cite elements
- _pull-quote.css: Fraunces display type, 3px teal ::before accent bar, responsive font-size at 905px+
- main.css: three @import lines added after _reveal.css, no existing imports disturbed
- Zero hex values in any of the three new files
</success_criteria>

<output>
Create .planning/phases/05-case-study-components/05-01-SUMMARY.md when done.
</output>
