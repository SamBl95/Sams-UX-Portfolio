---
phase: 05-case-study-components
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - src/styles/3-components/_process-steps.css
  - src/styles/3-components/_before-after.css
  - src/styles/3-components/_metrics-row.css
  - src/styles/3-components/_two-column.css
  - src/styles/main.css
autonomous: true
requirements:
  - CS-04
  - CS-05
  - CS-06
  - CS-07
  - CS-08

must_haves:
  truths:
    - "process-steps counter auto-increments from 1 using CSS counter-reset/counter-increment"
    - "Step counter renders as a 40x40px circle with teal border and Fraunces numeral"
    - "Connector line between steps appears at 905px+ and is hidden on last item"
    - ".before-after__col--before chip has a neutral border; .before-after__col--after chip has a teal border and accent-light background"
    - "before-after stacks vertically on mobile and renders as a 1fr/1fr grid at 905px+"
    - "metrics-row renders 2-col grid on mobile and flex row with border-left dividers at 905px+"
    - ".metrics-row__value matches the .stat-block__value pattern: Fraunces bold, teal, -0.02em tracking, accent underline via ::after"
    - ".two-col renders column on mobile and 1fr/1fr grid at 905px+; .two-col--asymmetric is 2fr/1fr"
    - "All 4 new CSS files are imported in main.css"
  artifacts:
    - path: "src/styles/3-components/_process-steps.css"
      provides: "Numbered process list with circle counter and connector line"
      contains: "counter-reset: step"
    - path: "src/styles/3-components/_before-after.css"
      provides: "Two-column before/after comparison with labelled chips"
      contains: ".before-after__col--after"
    - path: "src/styles/3-components/_metrics-row.css"
      provides: "Inline metrics display mirroring stat-block pattern"
      contains: ".metrics-row__value"
    - path: "src/styles/3-components/_two-column.css"
      provides: "Layout utility for 1fr/1fr and 2fr/1fr grids"
      contains: ".two-col--asymmetric"
  key_links:
    - from: "src/styles/main.css"
      to: "src/styles/3-components/_process-steps.css"
      via: "@import statement after _pull-quote.css (added by plan 01)"
      pattern: "@import.*_process-steps\\.css"
    - from: "src/styles/3-components/_metrics-row.css"
      to: "src/styles/3-components/_stat-block.css"
      via: "identical ::after underline pattern — must match exactly"
      pattern: "metrics-row__value::after"
---

<objective>
Create four CSS files for case study page layout components: process-steps (numbered list with connector), before-after (comparison columns with chips), metrics-row (inline stat display), and two-column (layout utility). Wire all four into main.css.

Purpose: Complete the Phase 5 component set so case study pages have the full storytelling toolkit available without further CSS work.
Output: Four new CSS files under src/styles/3-components/ plus four new @import lines in main.css.
</objective>

<execution_context>
@C:/Users/sambl/OneDrive/Documents/my-portfolio-v2/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/sambl/OneDrive/Documents/my-portfolio-v2/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@C:/Users/sambl/OneDrive/Documents/my-portfolio-v2/.planning/ROADMAP.md
@C:/Users/sambl/OneDrive/Documents/my-portfolio-v2/.planning/phases/05-case-study-components/05-UI-SPEC.md

<interfaces>
<!-- Established patterns the executor must replicate. Extracted from codebase. -->

From src/styles/3-components/_stat-block.css (metrics-row value must match this pattern exactly):
```css
.stat-block__value {
  font-size:      var(--text-4xl);
  font-weight:    var(--font-weight-bold);
  line-height:    1;
  letter-spacing: -0.02em;
  color:          var(--color-accent-accessible);
  position:       relative;
  padding-bottom: var(--space-3);
}

.stat-block__value::after {
  content:          '';
  position:         absolute;
  bottom:           0;
  left:             0;
  width:            var(--space-8);   /* 64px underline */
  height:           2px;
  background-color: var(--color-accent);
  border-radius:    var(--radius-full);
}

/* At 905px+: font-size: var(--text-5xl) */

.stat-block__label {
  font-size:   var(--text-sm);
  font-weight: var(--font-weight-normal);
  line-height: var(--leading-normal);
  color:       var(--color-text-secondary);
  max-width:   18ch;
}
```

From src/styles/main.css (current last component import — plan 01 will have added 3 more before this plan runs; if plan 01 has not run yet, insert after _reveal.css):
```css
/* After plan 01 runs, the block ends with: */
@import './3-components/_pull-quote.css';
```

From src/styles/1-settings/_variables.css:
```css
--radius-full: 9999px;
--radius-xl: 12px;
--space-5: 40px;   /* used as counter circle width/height */
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create _process-steps.css</name>
  <files>src/styles/3-components/_process-steps.css</files>
  <read_first>
    - src/styles/3-components/_stat-block.css (established accent color and font-heading usage pattern)
    - .planning/phases/05-case-study-components/05-UI-SPEC.md (CS-04 section — counter system and connector line)
    - src/styles/1-settings/_variables.css (spacing tokens, radius-full, font tokens)
  </read_first>
  <action>
    Create src/styles/3-components/_process-steps.css.

    Counter setup on the list:
    - .process-steps: counter-reset: step; list-style: none; padding: 0; margin: 0
    - .process-steps: display: flex; flex-direction: column; gap: var(--space-6) (mobile)
    - At 905px+: .process-steps gap: var(--space-8)

    Counter increment on each item:
    - .process-steps__item: counter-increment: step; position: relative

    Step counter circle via ::before:
    - .process-steps__item::before: content: counter(step)
    - display: flex; align-items: center; justify-content: center
    - width: var(--space-5); height: var(--space-5) (40px × 40px)
    - border-radius: var(--radius-full)
    - border: 2px solid var(--color-accent)
    - color: var(--color-accent-accessible)
    - font-family: var(--font-heading); font-size: var(--text-base)
    - font-weight: var(--font-weight-semibold); letter-spacing: -0.01em
    - flex-shrink: 0; margin-bottom: var(--space-3) (mobile — keeps circle above heading)

    Mobile item layout:
    - .process-steps__item: display: flex; flex-direction: column; gap: var(--space-3)

    Desktop item layout at 905px+:
    - .process-steps__item: display: grid; grid-template-columns: var(--space-5) 1fr
    - gap: var(--space-6); align-items: start
    - Remove margin-bottom from ::before at 905px+ (grid handles spacing)

    Connector line at 905px+ via ::after on .process-steps__item:
    - content: ''; position: absolute
    - left: calc(var(--space-5) / 2 - 1px) (centers 2px line under the 40px circle)
    - top: var(--space-5) (starts below counter circle)
    - width: 2px; height: calc(100% + var(--space-6)) (bridges gap to next item)
    - background-color: var(--color-accent); opacity: 0.3
    - Hide on last item: .process-steps__item:last-child::after { display: none }
    - The ::after connector only renders at 905px+ — do not add it in mobile styles

    Heading (.process-steps__heading):
    - font-size: var(--text-base); font-weight: var(--font-weight-semibold)
    - line-height: var(--leading-normal); color: var(--color-text-primary)
    - margin: 0 0 var(--space-1) 0

    Body (.process-steps__body):
    - font-size: var(--text-base); font-weight: var(--font-weight-normal)
    - line-height: var(--leading-relaxed); color: var(--color-text-secondary); margin: 0
  </action>
  <verify>
    <automated>grep -c "counter-reset: step" "C:/Users/sambl/OneDrive/Documents/my-portfolio-v2/src/styles/3-components/_process-steps.css"</automated>
  </verify>
  <acceptance_criteria>
    - _process-steps.css contains counter-reset: step on .process-steps
    - _process-steps.css contains counter-increment: step on .process-steps__item
    - _process-steps.css contains .process-steps__item::before with content: counter(step), width: var(--space-5), height: var(--space-5), border-radius: var(--radius-full), border: 2px solid var(--color-accent)
    - _process-steps.css contains an @media (min-width: 905px) block with .process-steps__item::after (connector line) set to position: absolute, width: 2px, background-color: var(--color-accent), opacity: 0.3
    - _process-steps.css contains .process-steps__item:last-child::after { display: none }
    - No hex values present
  </acceptance_criteria>
  <done>_process-steps.css created with CSS counter system, 40px circle pseudo-elements, and 905px+ connector line, matching CS-04 spec.</done>
</task>

<task type="auto">
  <name>Task 2: Create _before-after.css and _metrics-row.css</name>
  <files>
    src/styles/3-components/_before-after.css
    src/styles/3-components/_metrics-row.css
  </files>
  <read_first>
    - src/styles/3-components/_stat-block.css (metrics-row value and ::after underline must match this exactly — read before writing a single line of _metrics-row.css)
    - .planning/phases/05-case-study-components/05-UI-SPEC.md (CS-05 section for before-after; CS-06 section for metrics-row)
    - src/styles/1-settings/_variables.css (radius tokens, color tokens, spacing tokens)
  </read_first>
  <action>
    Create src/styles/3-components/_before-after.css:

    Outer wrapper (.before-after):
    - Mobile: display: flex; flex-direction: column; gap: var(--space-4)
    - At 905px+: display: grid; grid-template-columns: 1fr 1fr; gap: var(--space-8); align-items: start

    Column (.before-after__col):
    - background-color: var(--color-surface)
    - border: 1px solid var(--color-border)
    - border-radius: var(--radius-xl)
    - padding: var(--space-6)
    - display: flex; flex-direction: column; gap: var(--space-3)

    Label chip (.before-after__label):
    - display: inline-flex; align-items: center
    - padding: 4px var(--space-2) (4px top/bottom is a documented exception, not a token miss)
    - border-radius: var(--radius-full)
    - font-size: var(--text-xs); font-weight: var(--font-weight-semibold)
    - letter-spacing: 0.06em; text-transform: uppercase

    Before chip (.before-after__col--before .before-after__label):
    - border: 1px solid var(--color-border)
    - color: var(--color-text-secondary)
    - background-color: transparent

    After chip (.before-after__col--after .before-after__label):
    - border: 1px solid var(--color-accent)
    - color: var(--color-accent-accessible)
    - background-color: var(--color-accent-light)

    ---

    Create src/styles/3-components/_metrics-row.css:

    This component is a deliberate parallel to _stat-block.css. The distinction is context: metrics-row is embedded in case study body prose (no full-bleed surface background). The value and ::after underline must be identical to .stat-block__value.

    List reset (.metrics-row):
    - list-style: none; padding: 0; margin: 0
    - Mobile: display: grid; grid-template-columns: repeat(2, 1fr); gap: var(--space-8) var(--space-6)
    - At 905px+: display: flex; flex-direction: row; gap: 0

    Item at 905px+ (.metrics-row__item):
    - padding-inline: var(--space-8)
    - border-left: 1px solid var(--color-border)
    - First child: padding-left: 0; border-left: none
    - Last child: padding-right: 0

    Value (.metrics-row__value) — must match .stat-block__value exactly:
    - font-family: var(--font-heading)
    - font-size: var(--text-4xl); font-weight: var(--font-weight-bold)
    - line-height: 1; letter-spacing: -0.02em
    - color: var(--color-accent-accessible)
    - position: relative; padding-bottom: var(--space-3)
    - At 905px+: font-size: var(--text-5xl)

    Decorative underline (.metrics-row__value::after):
    - content: ''; position: absolute; bottom: 0; left: 0
    - width: var(--space-8); height: 2px
    - background-color: var(--color-accent); border-radius: var(--radius-full)

    Label (.metrics-row__label):
    - font-size: var(--text-sm); font-weight: var(--font-weight-normal)
    - line-height: var(--leading-normal); color: var(--color-text-secondary)
    - max-width: 18ch
  </action>
  <verify>
    <automated>grep -c "before-after__col--after" "C:/Users/sambl/OneDrive/Documents/my-portfolio-v2/src/styles/3-components/_before-after.css" && grep -c "metrics-row__value::after" "C:/Users/sambl/OneDrive/Documents/my-portfolio-v2/src/styles/3-components/_metrics-row.css"</automated>
  </verify>
  <acceptance_criteria>
    - _before-after.css contains .before-after with flex-direction: column mobile and grid-template-columns: 1fr 1fr at 905px+
    - _before-after.css contains .before-after__col with background-color: var(--color-surface), border: 1px solid var(--color-border), border-radius: var(--radius-xl), padding: var(--space-6)
    - _before-after.css contains .before-after__col--before .before-after__label with color: var(--color-text-secondary) and border: 1px solid var(--color-border)
    - _before-after.css contains .before-after__col--after .before-after__label with border: 1px solid var(--color-accent) and background-color: var(--color-accent-light)
    - _metrics-row.css contains .metrics-row__value with font-family: var(--font-heading), color: var(--color-accent-accessible), letter-spacing: -0.02em, position: relative, padding-bottom: var(--space-3)
    - _metrics-row.css contains .metrics-row__value::after with position: absolute, bottom: 0, left: 0, width: var(--space-8), height: 2px, background-color: var(--color-accent), border-radius: var(--radius-full)
    - _metrics-row.css contains @media (min-width: 905px) with .metrics-row__item having border-left: 1px solid var(--color-border) and first-child override removing border-left
    - No hex values in either file
  </acceptance_criteria>
  <done>_before-after.css with differentiated Before/After chips and _metrics-row.css with stat-block-matching value pattern created, matching CS-05 and CS-06 specs.</done>
</task>

<task type="auto">
  <name>Task 3: Create _two-column.css and wire all 4 imports into main.css</name>
  <files>
    src/styles/3-components/_two-column.css
    src/styles/main.css
  </files>
  <read_first>
    - src/styles/main.css (current state — identify the end of the 3. COMPONENTS block to insert after)
    - .planning/phases/05-case-study-components/05-UI-SPEC.md (CS-07 section; CS-08 section for exact import order)
  </read_first>
  <action>
    Create src/styles/3-components/_two-column.css:

    This is a layout utility only — no background, border, typography, or color declarations.

    Base (.two-col):
    - Mobile: display: flex; flex-direction: column; gap: var(--space-8)
    - At 905px+: display: grid; grid-template-columns: 1fr 1fr; gap: var(--space-8); align-items: start

    Asymmetric modifier (.two-col--asymmetric) at 905px+:
    - grid-template-columns: 2fr 1fr

    Vertical alignment modifier (.two-col--valign-center) at 905px+:
    - align-items: center

    Both modifiers are 905px+ only — on mobile, all .two-col variants stack as flex column.

    ---

    Update src/styles/main.css:

    Add four @import lines after the existing imports in the /* 3. COMPONENTS */ block. After plan 01 runs, the last component import will be @import './3-components/_pull-quote.css';. If plan 01 has not run and _pull-quote.css is not yet imported, insert after @import './3-components/_reveal.css'; instead.

    Add in this order:
    1. @import './3-components/_process-steps.css';
    2. @import './3-components/_before-after.css';
    3. @import './3-components/_metrics-row.css';
    4. @import './3-components/_two-column.css';

    Do not modify or reorder any existing import lines. The /* 4. LAYOUTS */ block remains unchanged below.
  </action>
  <verify>
    <automated>grep -c "_process-steps\|_before-after\|_metrics-row\|_two-column" "C:/Users/sambl/OneDrive/Documents/my-portfolio-v2/src/styles/main.css"</automated>
  </verify>
  <acceptance_criteria>
    - _two-column.css contains .two-col with flex-direction: column mobile and grid-template-columns: 1fr 1fr at 905px+
    - _two-column.css contains .two-col--asymmetric with grid-template-columns: 2fr 1fr inside a 905px media query
    - _two-column.css contains .two-col--valign-center with align-items: center inside a 905px media query
    - _two-column.css contains no color, background, border, font-size, or typography declarations
    - main.css contains @import './3-components/_process-steps.css';
    - main.css contains @import './3-components/_before-after.css';
    - main.css contains @import './3-components/_metrics-row.css';
    - main.css contains @import './3-components/_two-column.css';
    - All four new imports appear within the /* 3. COMPONENTS */ block
    - Existing imports are unchanged and in their original order
  </acceptance_criteria>
  <done>_two-column.css created as a layout-only utility with asymmetric modifier, and main.css updated with all 4 @import lines from this plan.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| CSS file authoring | All files are authored CSS — no user input, no dynamic content, no external requests |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05b-01 | Information Disclosure | CSS custom properties | accept | Custom property values are design tokens only; no PII or secrets |
| T-05b-SC | Tampering | npm/pip installs | accept | No package installs in this plan — pure CSS authoring |
</threat_model>

<verification>
After all tasks complete:

1. All four CSS files exist:
   - src/styles/3-components/_process-steps.css
   - src/styles/3-components/_before-after.css
   - src/styles/3-components/_metrics-row.css
   - src/styles/3-components/_two-column.css

2. main.css import count (combined with plan 01's 3 imports = 7 total):
   grep -c "_image-block\|_callout\|_pull-quote\|_process-steps\|_before-after\|_metrics-row\|_two-column" src/styles/main.css
   Expected: 7

3. No hex values in new files:
   grep -rn "#[0-9a-fA-F]\{3,6\}" src/styles/3-components/_process-steps.css src/styles/3-components/_before-after.css src/styles/3-components/_metrics-row.css src/styles/3-components/_two-column.css
   Expected: 0 matches

4. Counter system:
   grep "counter-reset\|counter-increment" src/styles/3-components/_process-steps.css
   Expected: both present

5. Two-column purity check (no typography/color):
   grep -c "color\|font\|background" src/styles/3-components/_two-column.css
   Expected: 0
</verification>

<success_criteria>
- _process-steps.css: CSS counter system with 40px circle ::before, connector line ::after at 905px+, last-child connector hidden
- _before-after.css: two-column comparison at 905px+, differentiated Before (neutral) and After (teal) label chips
- _metrics-row.css: 2-col grid mobile, flex row with border-left dividers at 905px+, value pattern identical to _stat-block.css
- _two-column.css: layout utility only, 1fr/1fr and 2fr/1fr grid modifiers, no color or typography declarations
- main.css: four @import lines added, all 7 Phase 5 component imports present when combined with plan 01 output
</success_criteria>

<output>
Create .planning/phases/05-case-study-components/05-02-SUMMARY.md when done.
</output>
