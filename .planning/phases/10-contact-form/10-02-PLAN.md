---
phase: 10-contact-form
plan: 02
type: execute
wave: 2
depends_on:
  - 10-01
files_modified: []
autonomous: false
requirements:
  - FORM-05

must_haves:
  truths:
    - "FormSubmit endpoint for sam.blake@outlook.com has been activated via the confirmation email"
    - "A test submission from the live contact form delivers a message to sam.blake@outlook.com inbox"
  artifacts:
    - path: "sam.blake@outlook.com inbox"
      provides: "Activation confirmation email from FormSubmit"
  key_links:
    - from: "contact form on site"
      to: "sam.blake@outlook.com"
      via: "FormSubmit AJAX endpoint activation"
      pattern: "n/a — manual activation step"
---

<objective>
Activate the FormSubmit endpoint and verify end-to-end email delivery.

Purpose: FormSubmit requires a one-click activation before it will relay form submissions. This plan walks through the activation step and confirms delivery works.
Output: Activated FormSubmit endpoint; verified test email received.
</objective>

<execution_context>
@C:/Users/sambl/OneDrive/Documents/my-portfolio-v2/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/sambl/OneDrive/Documents/my-portfolio-v2/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Trigger activation by submitting a test POST to the FormSubmit endpoint</name>
  <files></files>
  <read_first>
    .planning/STATE.md — confirm email target is sam.blake@outlook.com and no dependencies have changed
  </read_first>
  <action>
    Run the following curl command to send the first POST to the FormSubmit AJAX endpoint.
    This triggers FormSubmit to send an activation email to sam.blake@outlook.com.

    curl -X POST https://formsubmit.co/ajax/sam.blake@outlook.com \
      -H "Accept: application/json" \
      -H "Content-Type: application/json" \
      -d '{"name":"Activation Test","email":"sam.blake@outlook.com","message":"Activation trigger — please ignore."}'

    Expected response: FormSubmit returns either an activation-pending message or a 200 JSON response.
    If the endpoint has never been used before, FormSubmit sends an activation email to sam.blake@outlook.com before relaying any messages.

    Record the HTTP response status and body in this plan's SUMMARY.
  </action>
  <verify>
    curl exits without connection error. HTTP response is received (status 200 or 422 are both normal for a new endpoint pending activation).
  </verify>
  <acceptance_criteria>
    - curl command completes without a network/DNS error
    - Response body is valid JSON (not an HTML error page)
  </acceptance_criteria>
  <done>Activation POST sent; FormSubmit has received the first request and dispatched the activation email</done>
</task>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 2: Click activation link in sam.blake@outlook.com inbox</name>
  <what-built>The curl in Task 1 triggered FormSubmit to send an activation email to sam.blake@outlook.com. The endpoint will NOT relay messages until you click the link.</what-built>
  <how-to-verify>
    1. Open sam.blake@outlook.com in Outlook or a browser.
    2. Look for an email from noreply@formsubmit.co with subject "FormSubmit — Confirm your email address" (may take 1–3 minutes; check spam).
    3. Click the "Activate Form" button or link inside the email.
    4. You should see a confirmation page at formsubmit.co confirming the endpoint is active.
    5. Return here and type "activated" to continue.
  </how-to-verify>
  <resume-signal>Type "activated" once you have clicked the confirmation link</resume-signal>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Verify end-to-end form delivery on the live site</name>
  <what-built>FormSubmit endpoint is now activated. This checkpoint confirms a real browser submission delivers email.</what-built>
  <how-to-verify>
    1. Run `npx vite` (or open the dev server if already running).
    2. Navigate to http://localhost:5173/src/pages/contact.html (or the dev server URL for the contact page).
    3. Fill in the form:
       - Name: Test Submission
       - Email: sam.blake@outlook.com
       - Message: End-to-end test — form is live.
    4. Click "Send message".
    5. Confirm the success panel ("Message sent") appears on the page and the form disappears — NO page navigation.
    6. Check sam.blake@outlook.com inbox — a message from noreply@formsubmit.co with the submitted content should arrive within 1–2 minutes.
    7. Optionally, test error state: disable network in DevTools (Network tab → Offline), submit the form again — confirm the error panel appears without hiding the form.
  </how-to-verify>
  <resume-signal>Type "verified" once email is received and success state confirmed, or describe any issues</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Sam's inbox → FormSubmit confirmation link | Activation link is single-use and delivered to the verified inbox only |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-10-06 | Spoofing | FormSubmit activation email | accept | Activation email goes to sam.blake@outlook.com — attacker cannot activate unless they control that inbox |
| T-10-07 | Information Disclosure | curl command exposes email in shell history | accept | Email is already public in contact.html mailto; equivalent exposure |
| T-10-SC | Tampering | npm/pip installs | accept | No package installs in this plan |
</threat_model>

<verification>
1. curl to formsubmit.co/ajax returns valid JSON — confirms network path is open
2. Activation email arrives at sam.blake@outlook.com and is confirmed clicked
3. Test form submission from dev server shows in-page success state (no navigation)
4. Test submission appears in sam.blake@outlook.com inbox within 2 minutes
</verification>

<success_criteria>
- FormSubmit endpoint for sam.blake@outlook.com is activated (confirmed via activation email click)
- A browser-submitted form produces an email delivered to sam.blake@outlook.com
- In-page success state visible after submission, no page reload
- FORM-05 satisfied
</success_criteria>

<output>
Create `.planning/phases/10-contact-form/10-02-SUMMARY.md` when done
</output>
